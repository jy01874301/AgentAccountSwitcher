# 全量审计报告（2026-09-20）

> 起因：双通道改造（国服 `wb` / 国际服 `wbai`）落地后，用户要求
> 「逐一检查当前代码或方案中是否仍存在遗漏的问题，若发现任何问题，则进行修复、
> 改进和优化，并简要说明每处修改的原因与影响」。
>
> 结果：**共 22 项发现，全部已修复**。回归自检 **306 → 392 项全 PASS**，
> 退出码 0。三个真实登录态（国服 / 国际服 / Trae）全程 sha256 未变。
>
> 上一份见 `AUDIT_2026-09-19.md`（那份的 6 条建议当时已全部关闭，
> 本轮补齐了它「未能验证」列表里的两条）。

---

## 一、通道隔离类（问题 1–7、9–10、15–17）

双通道改造的核心风险是「一个通道的操作落到另一个通道的数据上」。
这一类共 12 项，是本次审计的主体。

| # | 问题 | 原因 → 影响 | 修改 |
|---|---|---|---|
| 1 | `Channel.auth_dir` 是 `__init__` 里的路径快照 | 冻结态启动时 `_BIN_DIR` 才被改指 exe 目录，快照收不到 → **打包后 exe 恒显示 0 个账号**，源码运行却正常 | 改成按 `_BIN_DIR` 现算的 `@property` |
| 2 | `AUTH_DIR` 模块常量（已退化为快照别名） | 留着就诱使下一个人去改一个没人读的名字 | 删除，原处留注释说明为何不留 |
| 3 | `/api/open-client` 不分通道 | 客户端进程按国服 exe 名枚举，无区分手段 → `/api/wbai/open-client` 会**拉起国服客户端** | 加 `open_client` 能力开关 + HTTP 层守卫 |
| 4 | `/api/client-status` 国际服谎报国服进程 | 把国服状态当国际服的返回，用户会误判 | 国际服返回 `client:null, unsupported:true` |
| 5 | `migrate_preview` 能力守卫只写在 HTTP 层 | `--channel wbai --migrate-preview` 能绕过 → **泄露一份国服数据预览**（`root`/`sessions` 全是国服的） | 守卫下沉到函数内，CLI 与 HTTP 只有一个真相来源 |
| 6 | `refresh_all` 守卫只在 HTTP 层 | `--channel wbai --refresh-all` 会逐个账号刷屏假失败 | 同上，下沉到函数内 |
| 7 | `checkin_all` / `refresh_all_ui` 不传通道，硬编码默认（国服） | HTTP 层那道 400 只挡 `/api/wbai/checkin`；任何别处调用都会去**签国服的账号** | 改为显式收 `ch` 参数 |
| 9 | `--migrate-preview` 退出码恒为 0 | 能力被拒时 JSON 里 `ok=false`，但脚本用 `$?` 会当成成功 | 拒绝时补 `unsupported:true`，CLI 据此返回 1 |
| 10 | `--channel wbai --switch x --migrate` 静默丢弃 migrate | 返回 `ok=true` + `migration=null`，用户以为数据也迁过去了 | 返回消息里明说「国际服不支持本地数据迁移，本次仅切换」 |
| 15 | `credits_snapshot` / `checkin_snapshot` / `checkin_account_file` 内部写死默认通道 | 与 #7 同类。国服是唯一有这些能力的通道，故现网无症状；能力一开放就**静默读错账号库** | 三个都加 `ch=None`，内部 `ch = channel(ch)` |
| 16 | `_CREDITS_CACHE` / `_CHECKIN_CACHE` 是模块级扁平单槽 | 比 #15 更隐蔽：**即使 #15 修好，缓存不分区照样把国服结果喂给国际服** | 改成 `{通道 key: {ts, payload}}`；失效只 `pop` 该通道 |
| 17 | `checkin_account_file` 锁名 `"wb-auth-"+base`，与另外 3 处不一致 | 两个通道的账号库是不同目录，同名文件不该互相阻塞 | 改 `ch.key + "-auth-" + base`（wb 侧拼出字符串完全一样，向后兼容） |

---

## 二、数据目录基准类（问题 19–20）

**背景**：这个坑在本项目已经造成两次真实损失 —— 一次让自检真的切了本机登录态，
一次让打包后的 exe 跑去 `_internal\wb_auth\` 找账号（页面恒显示 0 个）。
根源是「模块级路径常量 = 导入时的快照」，而冻结态改写发生在导入**之后**。

| # | 问题 | 原因 → 影响 | 修改 |
|---|---|---|---|
| 19 | `tw_ui_server` 半新半旧：`TW_AUTH_DIR` / `LOCK_DIR` 是导入时快照，而 `tw_backups` 是调用时现算 | 启动器要记着赋 4 行，**漏一行就是「账号库在一个目录、备份在另一个目录」的静默分家**；同一文件里两种写法最容易骗过 review | 改成 `auth_dir()` / `lock_dir()` / `backup_dir()` 三个函数，都按 `_BIN_DIR` 现算 |
| 20 | 改写基准散在 5 个名字上，靠启动器逐个赋值 | 逐个赋值就是「漏改一个」的来源 —— `AUTH_DIR` 就是这么退化成快照别名的 | 新增 `rebind(base_dir[, script_dir])`，一次改全；启动器里**一行 `srv.X = ...` 都不许有** |

---

## 三、可用性 / 交互类（问题 12–13）

| # | 问题 | 原因 → 影响 | 修改 |
|---|---|---|---|
| 12 | ⚠️ **`WorkBuddySwitcher.exe` 是桌面启动器，不认 CLI 动作 —— 会静默弹一个窗口** | `_parse_args` 只认 `--serve/--no-window/--port`，其余参数全被丢弃。`WorkBuddySwitcher.exe --refresh-all` 不会续期，而是开一个 GUI 窗口；前台 shell 会一直等它（CI/脚本表现为卡住），退出码还是 0，计划任务会当成成功 | `_SERVER_ACTIONS` 命中就 `return srv.main()`；`--channel`/`--force`/`--migrate-mode` 只算修饰符，单独出现仍走开窗口 |
| 13 | ⚠️ `smoke_test.py` 里函数内 `import ast` 遮蔽模块级 `ast` | Python 里函数内 import 会让该名字在**整个函数**内变局部变量 → 第 418 行的 `ast.Module(...)` 直接 `UnboundLocalError`，而 `import ast` 写在 710 行，**报错点离根因 300 多行** | 删掉函数内那句（顶部已有）；`inspect` 同理 |

> 已核查：`refresh_all.cmd` / `install_refresh_task.ps1` 调的是
> `python wb_ui_server.py`，**没有**踩到 #12。

---

## 四、测试覆盖缺口（问题 21）

| # | 问题 | 原因 → 影响 | 修改 |
|---|---|---|---|
| 21 | ⚠️ `tw.switch_account` —— **本工具里唯一会重写客户端登录态的代码 —— 此前零测试覆盖** | wb 侧有 §5b/§5c，tw 侧一直空着。上面那个 `UnboundLocalError` 就落在这条路径上，没有用例只能等用户真去切号才发现 | 新增 §5e：全程临时目录 + **伪造的**本机登录态（`_appdata_dir` 重定向 + `rebind`），覆盖切号成功 / 备份内容正确 / 目标密文写入且保留本机其它键 / `remove` 闭环 |

---

## 五、文档与测试漂移（问题 8、11、14、18、22）

| # | 问题 | 修改 |
|---|---|---|
| 8 | `tw_ui_server` 陈旧注释、`smoke_test` §10 用「自测副本」代替真实常量、`check_ttl.py` 漏了 `wbai_auth` 扫描目录、README 多处、模块 docstring | 逐处修正；断言改为测**真实常量**而非副本 |
| 11 | `check_ttl.py` docstring 与 README 的扫描目录说明没跟上 | 补 `wbai_auth` |
| 14 | 「打包后才会暴露」的问题只能拿真 exe 验，但验证手法没沉淀 | 新增 `check_exe_datadir.py`（双探针法，wb + wbai 两通道），已进 spec `datas`；README 补「exe 数据目录基准」章节 |
| 18 | `DESIGN_account_migration.md` / `DESIGN_single_instance.md` 里「总计 159/189 项全 PASS」是当时快照，会被误读成当前值；`AUDIT_2026-09-19.md` 的「未能验证」两条已补齐 | 标注「当时」+ 指向 README；更新那两条为已补齐（含验证手法） |
| 22 | 断言「源码里没有 X」时**没先剥注释** —— 本会话第 3 次栽在这 | 新增 `code_only(src)` 剥注释 + 用 `PATH_NAMES` 白名单精确列举，而不是「任何 `srv.X =`」（后者还误伤了合法的 `srv.Handler.TOKEN = ...`） |

---

## 六、验证

| 项 | 结果 |
|---|---|
| 回归自检 | **392 PASS / 0 FAIL**，退出码 0（本轮起算 306 → 392） |
| 冻结态双探针（wb） | `[wb] ['workbuddy-probe-exe.info']`、`[wbai] ['workbuddyai-probe-exe.info']` —— 都读 exe 同级，`_internal` 探针一个没被读到 |
| 冻结态双探针（tw） | `['trae-probe-exe.json']` —— 同上 |
| 冻结态页面 | `/` 两个入口齐全；`/wb` `/wbai` 无残留占位符；`wbai credits` 带 `unsupported` |
| 冻结态 CLI | `--channel wbai --migrate-preview x.info` 打印 JSON 并返回 1，不再开窗口 |
| 原生窗口 | 两个 exe 无参数启动：`tasklist /V` 有窗口标题、进程存活、`desktop-start.log` 无「WebView 启动失败」 |
| 真实国服登录态 | sha256 `f472b05f3b49d53e` 未变 |
| 真实国际服登录态 | sha256 `0ea1c33adbf46bfa` 未变 |
| 真实 Trae 登录态 | sha256 `c6c687dbebfa4189` 未变；`tw_backups\` 仍只有 09-10 的两份旧备份，**没有今日文件**（任何真切号都会在那里留备份 —— 这是决定性证据） |
| 产物 | `dist\WorkBuddySwitcher\`、`dist\TraeSwitcher\` 均已重建；无探针/凭据残留；无残留进程 |

---

## 七、仍未验证（诚实说明）

| 项 | 原因 |
|---|---|
| 真实数据迁移（把旧账号名下数据改归属到新账号） | 会动真实对话数据。自检用临时目录完整覆盖了迁移 + 回滚，但没对真实账号执行 |
| 「迁移前自动关闭客户端」的真实执行 | `WorkBuddyAI.exe` 就是托管本对话的客户端，真关会中断会话。已用 mock 覆盖 4 个分支 |
| 国际服的积分 / 签到 / 续期 / 迁移 | ~~网关与国服不同、未经验证 —— 按设计**一律不显示、不调用**（`Channel.supports()` 门控）。这是能力取舍，不是缺陷~~ **已于当日 17:28 更新，见下方** |

> **2026-09-20 17:28 更新（本表原结论已作废）**
>
> 原结论基于「国际服网关未验证」，实测后发现**当时是打错了网关**：
> 国服是 `https://copilot.tencent.com`，国际服是 `https://www.workbuddy.ai`。
> 拿国际服的 accessToken 打国服网关恒返回 `HTTP 401`，于是被误判成"网关不通用"。
>
> 换到国际服自己的网关后逐个实测：
>
> | 能力 | 结果 |
> |---|---|
> | 积分 `/v2/billing/meter/get-user-resource` | ✅ 可用（2/2 账号返回明细） |
> | 续期 `/v2/plugin/auth/token/refresh` | ✅ 可用（实测续期成功并写回） |
> | 签到 `/v2/billing/meter/checkin-activity-status` | 接口可达，但 `active=false`「签到活动未开启」 |
> | 迁移 | 仍关闭 —— 只认国服数据目录 `~/.workbuddy` |
>
> 代码改动：新增 `channel_cfg(ch)` 作为网关配置的唯一入口（原来 4 处各写一遍
> `wb.load_config(SCRIPT_DIR / "config.json")`，拿到的永远是国服网关）；
> `wbai.features` 打开 `credits` / `refresh`；预热改为覆盖所有通道。
> 自检 393 项全 PASS；两个 exe 已重打（`wb-20260920-175217`）并验证。

Trae 侧的 4 项已知遗留（跨设备素材无法远程续期、`refresh_all` 无门卫、
`_trae_tokens.json` 恒显示为不可用行、续期计划任务未注册）按用户
2026-09-18 的决定**暂不处理**。

---

## 八、两条值得复用的经验

1. **能力守卫必须写在函数内部**，不能只写在 HTTP 层 —— 本工具有 CLI 与 HTTP 两个入口，
   只挡一个等于没挡。同理，任何「按通道」的函数都必须显式收 `ch`，不靠默认值兜底；
   模块级缓存也必须按通道分区。
2. **「打包后才会暴露」的问题只能拿真 exe 验**。源码态怎么测都正常。
   所以把双探针固化成 `check_exe_datadir.py`，以后每次重打包跑一遍。
