# 全功能模块审计报告

**日期**：2026-09-21
**对象**：wb_switcher（WorkBuddy 双通道账号切换器 + Trae 姊妹工具）
**方式**：逐模块**实际运行**验证（非静态阅读），覆盖正常流程与边界情况
**基线对照**：`smoke_test.py` 394 PASS / 0 FAIL，退出码 0

---

## 一、结论速览

| # | 模块 | 状态 | 问题数 |
|---|---|---|---|
| 1 | 编译与回归自检 | ✅ | 0 |
| 2 | HTTP 骨架与安全守卫 | ✅ | 0（1 项设计观察） |
| 3 | 单实例保护 | ✅ | 0 |
| 4 | 路径与文件安全 | ✅ | 0 |
| 5 | 审计日志 `audit()` | ✅ | 0 |
| 6 | 账号切换（wb / wbai / tw） | ✅ | 0 |
| 7 | 前端模板与渲染 | ✅ | 0 |
| 8 | 国际服能力守卫 | ✅ | 0 |
| 9 | 数据迁移 `account_migration` | ✅ 已修 | P1 × 1（已修） |
| 10 | CLI 命令行（exe） | ✅ 已修 | P1 × 1（已修） |
| 11 | 性能：客户端状态探测 | ✅ 无误报 | 0（原报 P2 是误报） |
| 12 | 辅助脚本（import_token / check_ttl / 计划任务） | ✅ | 0 |

**总计**：2 个 P1 真实缺陷（均已修复并重打 exe 验证）、1 项 P3 设计观察（已补文档）、
1 项 P4 部署提示（已改为代码自动创建目录）。
**原报的 P2 性能瓶颈是误报** —— 缓存早已实现，详见第三章。

> **修复记录见第十章**。修复后回归自检 **403 PASS / 0 FAIL**（原 394 + 新增 9 条回归钉）。

---

## 二、P1 缺陷详情

### P1-1 迁移「校验」步骤误判 —— 成功却报失败，备份无限堆积

**位置**：`account_migration.py:709`

```python
journal.add_step("校验", not missing, detail)   # ← 缺陷
```

**触发条件**：会话在 DB 中有行，但 `projects/` 下找不到正文 jsonl。
（`cwd` 变过、或对话正文被清理过就会这样，真实场景并不罕见。）

**实测证据**（用真实 DB schema 的一致性副本，3 个会话其中 1 个无正文）：

```
ok=False   rolled_back=False
message : 迁移完成：前置检查✓；数据库快照✓；文件快照✓；数据库归属✓；账号快照✓；校验✗
warnings: ['1 个会话在 projects/ 下找不到正文（cwd 可能变过）：s3']
归属 NEW=3  归属 OLD=0        ← 数据实际已全部迁移成功
迁移备份份数 = 1
```

**两个实际危害**：

1. **报告与实际状态相反**。所有写操作都已成功且**未回滚**，但 `ok=False`。
   `switch_account` 据此把结果渲染成「⚠️ 但数据迁移失败」，用户会以为数据没迁过去，
   可能去做多余的补救操作。

2. **备份无限堆积**（更严重）。`account_migration.py:725`

   ```python
   if result["ok"]:
       prune_migration_backups(r)   # 失败时不裁剪 → 每轮都新增一份
   ```

   来回切 8 轮实测：

   ```
   备份份数 = 14（prune 上限应为 3）   合计 2200.9 KB
   ```

   每份 `.migration-backup/<时间戳>/workbuddy.db` 都是**完整对话库快照**。
   持续使用会不断占用磁盘，且这些快照含全部对话内容，属安全敏感数据。

**建议修法**（二选一或都做）：

- 把「正文缺失」从**失败**降级为**警告**：
  ```python
  journal.add_step("校验", True, detail)   # 缺失已在 result["warnings"] 里披露
  ```
  理由：`_verify_db` 已确认归属改写成功，「正文 jsonl 存在与否」与「归属是否正确」
  是两件独立的事，前者不该让整个迁移判为失败。

- 把裁剪从成功分支里摘出来：
  ```python
  if not result.get("rolled_back"):
      try: prune_migration_backups(r)
      except BaseException: pass
  ```
  理由：「不裁剪」的本意是保留回滚现场；未回滚时本就没有待恢复的现场。

---

### P1-2 exe CLI 在 GBK 控制台下 `UnicodeEncodeError` 崩溃

**位置**：`wb_ui_server.py` 第 1731 / 1734 / 1741 / 1745 行

```python
print(json.dumps({"accounts": list_accounts(ch)}, ensure_ascii=False, indent=2))
```

**触发条件**：windowed exe / 计划任务的 stdout 编码为 **GBK**，且账号昵称含
BMP 外字符（emoji）。本机 `wb_auth\workbuddy-hlqin.info` 昵称正是 `丹怡Helia 🌱`。

**实测证据**（`PYTHONIOENCODING=gbk` 模拟 exe 环境）：

| 命令 | 结果 |
|---|---|
| `--list` | **rc=1** `UnicodeEncodeError: 'gbk' codec can't encode character '\U0001f331'`（position 191） |
| `--current` | **rc=1** 同上（position 482） |
| `--switch <name>` | 同路径，同样崩溃 |
| `--migrate-preview <name>` | 同路径，同样崩溃 |
| `--channel wbai --list` | rc=0（国际服昵称无 emoji，**坑只在国服暴露**） |
| `tw_ui_server.py --list` | rc=0（Trae 侧无此写法） |

**为什么源码态测不出来**：Git Bash / `cmd` 交互式下 stdout 是 UTF-8，一切正常。
**只有 exe 直调或计划任务直调 exe 才暴露** —— 典型的环境相关缺陷。

**现状缓解**：`refresh_all.cmd` 调的是 `python wb_ui_server.py`，**绕过了 exe**，
所以每日续期任务目前不受影响。但 `WorkBuddySwitcher.exe --list` 用不了。

**建议修法**：加统一出口，四处替换：

```python
def _print_json(obj):
    """CLI 输出统一出口：GBK 控制台下 emoji 昵称会让 print 直接崩。"""
    text = json.dumps(obj, ensure_ascii=False, indent=2)
    try:
        sys.stdout.write(text + "\n")
    except UnicodeEncodeError:
        sys.stdout.reconfigure(encoding="utf-8", errors="replace")
        sys.stdout.write(text + "\n")
```

（或在 `main()` 开头一次性 `reconfigure(encoding="utf-8")` + try/except。
注意 `sys.stdout` 为 `None`（pythonw）时要跳过。）

---

## 三、P2 —— **误报，撤销**（2026-09-21 修正）

> **更正**：这一章的结论是错的。写报告时我依据的是**单次冷调用**的测量值，
> 而 `client_status()` 的缓存早已实现。核对源码后确认无需修改，实测如下。

**`client_status()` 已有 4 秒 TTL 缓存**，`wb_ui_server.py`：

- `_CLIENT_STATUS_CACHE` / `CLIENT_STATUS_TTL = 4.0`
- `invalidate_client_status()` —— 状态变更后主动失效
- `fresh=True` 参数 —— 动作接口（`/api/open-client`）绕过缓存
- `find_client_exe(procs)` **复用**已枚举的进程表，不再重复跑 `tasklist`

实测（本机）：

| 场景 | 耗时 |
|---|---|
| 冷调用（首次） | 0.290 s |
| **热调用（缓存命中）** | **0.000 s** |
| `fresh=True` | 0.306 s |
| `invalidate()` 之后 | 0.305 s |

**当时为什么误判**：探针 `_probe_perf.py` 只调用了一次 `client_status()`，
拿到的必然是冷启动的 599 ms；而 `/api/client-status` 与 `/api/open-client`
在真实使用中绝大多数是缓存命中的。教训是**测缓存要看第二次调用**。

**无需修改。** 以下为原始（错误）记录，保留备查：

---

**`client_status()` ≈ 599 ms**，无缓存，每次调用都重新枚举进程。

**耗时拆解**：

| 环节 | 耗时 |
|---|---|
| `common.list_processes()`（`tasklist` 子进程） | **284 ms / 次** |
| `find_client_exe()` | 304 ms |
| **`client_status()` 合计** | **≈ 599 ms** |

**问题**：`/api/client-status`（L1545）与 `/api/open-client`（L1562）**各自**调用一次
`client_status()`，都会重跑这两个 284ms/304ms 的子进程枚举。前端轮询或用户连点时
是纯粹的重复开销。

**对照**：同页面其他接口都很快 —— `list_accounts(wb)` 2.29ms、
`current_account(wb)` 4.32ms、`channel_cfg` 0.14ms、`build_ui_context` 0.00ms、
积分缓存命中 **0.0ms**（回源约 704ms）。唯独进程枚举完全没有缓存。

**建议修法**：给 `client_status()` 加 3~5 秒 TTL 缓存，与 `build_ui_context` 里
账号/积分的处置方式保持一致：

```python
_CLIENT_CACHE = {"ts": 0.0, "payload": None}
CLIENT_TTL = 4.0

def client_status(ch=None):
    now = time.time()
    if _CLIENT_CACHE["payload"] and now - _CLIENT_CACHE["ts"] < CLIENT_TTL:
        return _CLIENT_CACHE["payload"]
    ...  # 原逻辑
    _CLIENT_CACHE.update(ts=now, payload=out)
    return out
```

⚠️ 注意：`/api/open-client` 是**动作**接口，用户点「打开客户端」时期望立即拿到
最新状态。若要严格，可让 `open-client` 显式绕过缓存（`client_status(fresh=True)`），
只让只读的 `/api/client-status` 吃缓存。

---

## 四、P3 设计观察（非缺陷）

**`/api/ping` 与 `/api/page-alive` 位于同源守卫之前。**

`switcher_common.BaseHandler.do_GET` 中这两个路由在 `_guard()` **之前**处理，
因此 `Host: evil.com` 的请求仍会拿到 200。

- **这是有意设计**：单实例探测需要在跨源场景下也能确认「这个端口上是不是我们的服务」。
- **但** README 描述的是「Host / Origin 都必须回环」，措辞与实现不符。
- **建议**：在 README 里点明这两个端点是有意豁免的，避免后续维护者「修好」它反而
  破坏了单实例探测。
- **风险评估**：低。这两个端点只返回 `{ok, app, version, pid, port, page_open}`，
  不含任何账号数据。

---

## 五、P4 部署提示 —— 已改为代码自动创建

> **2026-09-21 已修**：不再需要手动建目录。exe 一跑（`--list` 或开窗口都行）
> 就会在同级建出 `wb_auth\` / `wbai_auth\` / `tw_auth\`，空目录里还会写一份
> `把账号文件放这里.txt`。详见第九章（`ensure_auth_dirs` / `ensure_auth_dir`）。

**原问题**：`dist/WorkBuddySwitcher/` 目录下**不含** `wb_auth/`（设计如此：账号库应与
exe 同级由用户自行放置）。部署时须把 `wb_auth/`、`wbai_auth/` 放到 **exe 同级目录**
（不是 cwd、不是 `_internal\`）。用户没建目录时页面显示「0 个账号」，
不知道该往哪放文件，看着像工具坏了。

**为什么不靠打包带上**：`dist/` 是构建产物，每次重新打包都会整个重建，
塞进去的空目录必然丢失；而部署时用户只拷 exe，不会手动建目录。

`check_exe_datadir.py` 已验证冻结态定位正确：

```
[wb]   ['workbuddy-probe-exe.info']
[wbai] ['workbuddyai-probe-exe.info']
失败项：无
```

---

## 六、逐模块验证明细（全部通过项）

### 6.1 编译与回归自检
- 11 个 `.py` 全部编译通过（Python 3.13.14）
- `smoke_test.py`：**394 PASS / 0 FAIL**，退出码 0

### 6.2 HTTP 骨架与安全守卫（66 PASS）
- 页面路由 `/` `/wb` `/wbai` 正常返回，模板注入令牌成功
- 只读接口全部正常；**14 个写端点用 GET 访问全部 405**
- 同源守卫：跨站 Origin → **403**
- 一次性令牌：缺失 / 错误 → **401**；首页与 `/wbai` 均正确注入
- **路径穿越 5 种全被拒**：`../` / `a/b.info` / `..\..\x.info` / `C:\Windows\x.info` / `sub/dir.info`
- 非 `.info` 后缀被拒
- `add` 闭环：合法通过 / 截断 token 拒 / 非法 JSON 拒 / 重名拒 / 空 name 拒
- **国际服写路径隔离**：只写 `workbuddyai-` 前缀，不碰国服库
- 国际服能力守卫：`checkin` / `open-client` → **400**
- 未知通道前缀 → 404；body 为数组不崩；query string 参数正常
- `/api/ping` 返回的身份信息正确

### 6.3 单实例保护
- `probe_instance` 能认出自己（pid / app / version / page_open 全对）
- `bind_server` 端口顺延：8905 → 8906
- **别人的 `/api/ping` 与非 JSON 响应不会被误认成自己**
- `page_is_open` 六种状态全对，含「心跳在 TTL 内、opened 已过期 → True」的分开判定
  （`PAGE_TTL=20s` 与 `PAGE_OPEN_GRACE=30s` 不取 max）

### 6.4 路径与文件安全
- `file_lock`：超时 **0.50s 精确**；锁文件恒 **1 字节**（不随加锁次数增长）；
  非 ASCII key 正确归一化
- `prune_backups`：删除份数正确、`exclude` 生效、`keep=0` 正常、
  目录不存在不抛、**非法 glob 不抛**
- `safe_unlink` / `safe_rmtree`：均不抛（含 `BaseException` 兜底，
  安全删除 shim 的 `SystemExit` 被正确吞掉）

### 6.5 审计日志 `audit()`
- 3 行格式正确；`detail` 截断至 160；换行被压平
- **不含凭据**（已验证）；嵌套目录自建；`None` / 空值不抛
- 达上限正确轮转为 `.1`

### 6.6 账号切换
- `token_looks_complete` / `token_source` / `scrub` / `same_origin` 边界 12 组全通过
- **Trae 侧真实素材**：3 个 `storage` 型素材全部 `complete=True`，uid 均可解析
  （`trae-account-c` / `trae-account-d` / `trae-account-e`，token_len 均 1004）
- Trae `switch_account`：正常切换 OK、生成备份、幂等、路径穿越 4 种全拒、
  非法 JSON 拒、无登录态字段拒、非 `.json` 后缀拒

### 6.7 前端模板与渲染
- `ui_template.html` 含 **17 个占位符**
- **三通道（wb / wbai / tw）全覆盖，零未覆盖项**
- 渲染后**零残留占位符**（长度 45119 / 45285 / 45018）
- 能力开关配置正确：

  | 开关 | wb | wbai | tw |
  |---|---|---|---|
  | CREDITS | 1 | 1 | 空 |
  | CHECKIN | 1 | **空** | 空 |
  | MIGRATE | 1 | **空** | 空 |
  | OPEN_CLIENT | 空 | 空 | 1 |
  | REFRESH | 1 | 1 | 1 |
  | API_BASE | `/api/` | `/api/wbai/` | `/api/` |

- **dist 两份 `ui_template.html`（WorkBuddySwitcher / TraeSwitcher）
  sha256 与源码完全一致**（`67fa1ae4bf06`），整体构建时间戳统一

### 6.8 数据迁移核心（机制部分正常）
- `preview` / `scan` 计数正确
- `move` 模式 5 个会话正确改归属
- **working 会话被正确跳过**
- `automations` 改归属正确；`account-snapshot` 刷新正确；`memory` 改名正确
- `share` 模式置空 5 条正确
- 同 uid 正确识别；**幂等**（第 2 次 `moved=0`）
- 备份带 `manifest.json`，正常路径下裁剪至 ≤3 份
- **回滚机制本身正常**：注入失败后 `rolled_back=True`、`state == before`
  （故障仅在「校验」步骤的判定上，见 P1-1）

### 6.9 辅助脚本
- `import_token.py` 8 组边界：正常解析、去重、坏 JSON 跳过、
  目标文件根节点非对象**拒绝写入**、输出路径为目录**拒绝**、
  非 dict 条目跳过、顶层裸对象、数组载荷 —— 全部正确（退出码语义正确）
- `check_ttl.py`：双目录 10 条输出正确，正确区分 `oneid_login`（52.8 天）/
  `enterprise_switch`（27.8~29.5 天）/ 无 `token_source` 按天数判定
- `window_state_guard.py check`：有漂移时 `exit=1`（正确）
- `refresh_all.cmd` / `trae_refresh_all.cmd`：入口与错误码传递正确

---

## 七、性能基线（实测）

| 操作 | 耗时 |
|---|---|
| `build_ui_context()` | 0.00 ms |
| `channel_cfg()` | 0.14 ms |
| `list_accounts(wbai)` | 0.64 ms |
| `list_accounts(wb)` | 2.29 ms |
| `current_uid(wb)` | 4.19 ms |
| `current_account(wb)` | 4.32 ms |
| `probe_instance_range(10 个空端口)` | 0.34 s |
| 积分缓存命中 | **0.0 ms**（回源约 704 ms） |
| **`client_status()`** | **≈ 599 ms** ← 瓶颈 |
| └ `common.list_processes()` | 284 ms |
| └ `find_client_exe()` | 304 ms |

---

## 八、当时的实施优先级建议 —— 已全部落实

| 优先级 | 项目 | 理由 | 结果 |
|---|---|---|---|
| **1** | P1-1 迁移校验判定 + 备份裁剪 | 会误导用户 + 持续占用磁盘 + 敏感数据堆积 | ✅ 源码已修，复测 `ok=True`、备份 14→3 份 |
| **2** | P1-2 CLI `_print_json` 兜底 | 4 条 CLI 命令在 exe 下完全不可用 | ✅ 源码已修 + **重打 exe**，CLI 全部 rc=0 |
| **3** | P2 `client_status` TTL 缓存 | 提升交互响应，改动小、风险低 | ⚠️ **误报**，缓存早已存在，无需改动 |
| **4** | P3 README 补注 ping 豁免说明 | 纯文档，防止后续误「修」 | ✅ 已补入 README「HTTP API」章节 |

**回归验证建议（当时提的，已照做）**：`smoke_test.py` 补了 9 条回归钉，
含「GBK 环境下 CLI 不崩」与「Trae 启动器 CLI 转发」。见第九章。

---

## 九、修复记录（2026-09-21 完成）

### 关于「审计过程中的错误弹窗」

用户反馈审计期间出现过几次弹窗，已查实：

| 项目 | 内容 |
|---|---|
| 弹窗标题 | **`Unhandled exception in script`** |
| 来源 | PyInstaller windowed exe 的标准 traceback 对话框 |
| 触发条件 | `console=False` + `disable_windowed_traceback=False` + 未捕获异常 |
| 根因 | exe CLI 崩于 `UnicodeEncodeError`（GBK stdout + 昵称 emoji `丹怡Helia 🌱`） |
| **放大危害** | 窗口版无控制台 → 弹**模态**框 → **进程阻塞等用户点确定**（计划任务里 = 永久挂起） |

复现与验证方法（`EnumWindows` 枚举顶层窗口标题 + 检查进程是否还活着）：

```
修复前:  TraeSwitcher.exe  --list      rc=124 超时  阻塞   （开 GUI 窗口）
修复前:  WorkBuddySwitcher.exe --current 阻塞  新增窗口: 'Unhandled exception in script'
修复后:  WorkBuddySwitcher.exe --list      rc=0   0.29s  弹窗=无
修复后:  WorkBuddySwitcher.exe --current  rc=0   0.31s  弹窗=无
修复后:  TraeSwitcher.exe     --list      rc=0   0.38s  弹窗=无
```

### 逐项修复

| 项 | 状态 | 动作 |
|---|---|---|
| **P1-1 迁移校验误判** | ✅ 已修（源码本就修好，本次复测确认） | `add_step("校验", True, ...)` + 裁剪条件改为 `if not result.get("rolled_back")` |
| **P1-2 CLI GBK 崩溃 → 弹窗** | ✅ 已修（源码本就修好，**缺的是重打 exe**） | `common.print_json` 兜底已存在；本次**重新打包两个 exe** |
| **P2 客户端状态性能** | ⚠️ **误报，撤销** | 缓存早已实现，无需改动 |
| **P3 ping 豁免无文档** | ✅ 已补 | README「HTTP API」章节加了例外说明，标注"有意豁免，不要修" |
| **P4 部署缺账号目录** | ✅ 已改 | 改为**代码自动创建**目录 + 写入占位说明文件 |
| **新增：Trae exe 的 CLI 不可用** | ✅ 已修 | `tw_ui_app.py` 缺 CLI 转发 → 加 `_SERVER_ACTIONS` / `_wants_server_cli`（与 wb 侧对齐） |

### 本次实际改动的文件

| 文件 | 改动 |
|---|---|
| `wb_ui_server.py` | 新增 `ensure_auth_dirs()`（P4）；`list_accounts()` / `serve()` 调用它 |
| `tw_ui_server.py` | 新增 `ensure_auth_dir()`（P4）；`list_accounts()` / `serve()` 调用它 |
| `tw_ui_app.py` | 新增 `_SERVER_ACTIONS` / `_wants_server_cli` 与 `main()` 里的转发（Trae exe CLI） |
| `README.md` | 「HTTP API」补 `/api/ping` / `/api/page-alive` 的同源豁免说明（P3） |
| `smoke_test.py` | 新增 9 条回归钉（GBK 兜底 / Trae CLI 转发 / 迁移校验 / 目录自动创建） |

**未改动**：`account_migration.py`、`switcher_common.py`、`wb_ui_server.main()` ——
这三处的修复在审计前已就位，本次只做验证。

### 重打的两个 exe

```
dist/WorkBuddySwitcher/WorkBuddySwitcher.exe   2026-09-21 05:51
dist/TraeSwitcher/TraeSwitcher.exe             2026-09-21 05:51
```

- 两份 `ui_template.html` sha256 与源码**一致**（`67fa1ae4bf06`）
- 构建时间戳统一，无半新半旧
- 两个 exe 的 CLI 均不弹窗、不阻塞、rc=0

### 新增回归钉（防止复发）

这三条坑的共同特点是「**只在 exe / 计划任务里暴露，源码态永远测不出来**」，
所以必须钉在自检里 —— 否则将来改回去时 smoke_test 一片绿，用户那边却静默炸掉。

| 断言 | 钉住什么 |
|---|---|
| `① CLI 输出统一走 common.print_json（有 GBK 兜底）` | 兜底函数存在且含 `UnicodeEncodeError` / `reconfigure` 处理 |
| `① GBK 流下输出 emoji 昵称不抛异常` | 真实往 GBK 流里写 `丹怡Helia 🌱`，不抛 |
| `① wb 侧 main() 不再裸 print(json.dumps(...))` | 防止有人改回裸 print |
| `② tw_ui_app 把一次性 CLI 动作转交后端（不开窗口）` | Trae exe CLI 转发不丢 |
| `② tw_ui_app 的 _SERVER_ACTIONS 覆盖 …` | 四个动词都在表里 |
| `③ 迁移校验步骤恒定判成功（正文缺失只进 warnings）` | `add_step("校验", True, ...)` |
| `③ 迁移备份按「未回滚」裁剪，而不是按 ok` | 防止备份堆积复发 |
| `④ wb 侧有 ensure_auth_dirs 且 serve 时调用` | P4 部署不退化 |
| `④ tw 侧有 ensure_auth_dir 且 serve 时调用` | 同上（Trae 侧） |

**回归结果：403 PASS / 0 FAIL，退出码 0**（原 394 + 新增 9）。

---

## 十、审计过程中发现的环境提示

- **`account_migration.ROOT_OVERRIDE` 语义**：它接受的是**数据目录本身**
  （即含 `workbuddy.db` 的那层，通常 `~/.workbuddy`），**不是**父目录。
  写测试时容易搞错，会得到 `数据库快照失败：找不到 workbuddy.db`。
- **`tw_ui_server` 没有 `build_ui_context`**：其上下文是
  `Handler.UI_CONTEXT` 类属性，与 wb 侧结构不同，验证时需分别取。
- `find_data_root()` 返回的路径可能是 Windows 短名形式（`ADMINI~1`），
  `Path.exists()` 可用但字符串比较不可靠。
