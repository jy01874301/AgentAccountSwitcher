# 单实例保护（设计说明）

> 目标：**任何时刻只允许一个切换器实例在跑**。第二个启动请求要么复用已有实例，
> 要么明确报错退出，**绝不静默另起一个**。
>
> 本文基于 2026-09-18 的实测，不是凭空设计。

---

## 0. 现状：完全没有保护，而且会静默起第二个

两处叠加导致"双击两次 = 两个实例"：

1. **没有任何单实例机制**。全仓 grep `mutex` / `single instance` / `CreateMutex`
   —— 零命中。
2. **`bind_server()` 端口被占用时静默顺延**：

   ```python
   def bind_server(handler_cls, port, host="127.0.0.1", tries=10):
       """绑定本地服务；端口被占用时自动顺延到下一个可用端口。"""
       for p in range(int(port), int(port) + int(tries)):
           if port_in_use(host, p): continue
           return QuietHTTPServer((host, p), handler_cls), p
   ```

   `wb_ui_server.py --serve` 与 `wb_ui_app.py` 都直接调它。于是第二次启动**不会报错**，
   而是安静地占用 8766、再开一个浏览器标签。

**实测复现**（2026-09-18 13:44）：

```
8765 -> pid 21728   （先启动的实例）
8766 -> pid 18944   （第二次启动的实例）
两个端口同时 LISTENING，两个首页都返回 200
```

### 这为什么是真问题，而不只是"多点一个窗口"

- **两个实例会各自持有一份一次性令牌**。从 8765 页面发出的写操作带的是 8765 的令牌，
  如果用户在新开的 8766 页面上操作，而页面是从 8765 加载的，写操作会被判 401。
- **两个实例可能跑着不同版本的代码**。本次会话就踩到过：8765 上是一个 02:16 启动的
  旧代码实例，它一直用**旧模板**服务，让人以为"改版没生效"；后来又把 `current_account()`
  的旧逻辑暴露成"当前账号显示成别人"。
- **两个实例会同时操作同一份登录态与账号库**。切号、续期、迁移都靠
  `.locks/wb-desktop.lock` 串行；跨进程锁虽然有效，但两个实例各自持有一份内存态，
  谁也不知道对方刚做了什么 —— 表现为"我刚切过去，它又给我切回来了"。
- **迁移尤其危险**：一个实例正在迁移（改 db + 改目录），另一个实例同时在切号，
  即使有锁，用户看到的状态也是混乱的。

---

## 1. 实现思路

### 1.1 主方案：Windows 命名互斥体

```python
import ctypes
from ctypes import wintypes

def acquire_single_instance(name="Local\\WorkBuddySwitcher"):
    """返回 (handle, already_running)。handle 必须活到进程结束。"""
    k32 = ctypes.WinDLL("kernel32", use_last_error=True)
    k32.CreateMutexW.restype = wintypes.HANDLE
    k32.CreateMutexW.argtypes = [wintypes.LPVOID, wintypes.BOOL, wintypes.LPCWSTR]
    h = k32.CreateMutexW(None, False, name)
    err = ctypes.get_last_error()
    if not h:
        return None, False          # 创建失败（权限等）→ 降级，不阻断启动
    return h, (err == 183)          # 183 = ERROR_ALREADY_EXISTS
```

**为什么用互斥体而不是 PID 锁文件**（已实测）：

| 场景 | 命名互斥体 | PID 锁文件 |
|---|---|---|
| 第一个实例正常退出 | 内核自动释放 | 需要清理，否则残留 |
| 第一个实例**被强杀 / 崩溃** | **内核自动释放** ✓ | 残留假锁，第二个起不来 |
| 进程被杀后 PID 被复用 | 不受影响 | 误判"实例还活着" |
| 需要额外清理逻辑 | 无 | 有 |

实测（两个独立进程）：

```
A 持有 5 秒 → B 启动：errno=183  → 已有实例（应退出）   ✓
A 退出后   → C 启动：errno=0    → 拿到所有权（可启动） ✓
A 被 kill -9 → 下一个：errno=0  → 拿到所有权（可启动） ✓  ← 关键优势
```

**命名空间选择**：

- `Local\...` —— 每个登录会话独立。**默认选它**：允许多用户/多 RDP 会话各跑一份，
  互不干扰（本机就是 RDP 会话 `RDP-Tcp#75`）。
- `Global\...` —— 全机器唯一。仅在确实要"整机只准一个"时用；
  创建 Global 对象需要权限，普通用户可能失败。

### 1.2 辅助：给服务加身份端点

光有互斥体还不够 —— 它只能告诉我们"**有**另一个实例"，不能告诉我们
"**那个实例在哪、是不是我们的**"。端口可能被别的程序占着，也可能被一个
**旧版本的自己**占着。

```python
# GET /api/ping  →  200
{"app": "wb_switcher", "source": "wb", "pid": 21728, "port": 8765,
 "version": "2026-09-18", "started_at": 1789000000}
```

`source` 用现有的 `Handler.SOURCE`（`wb` / `tw`），`version` 取
`git describe` 或一个写死的构建戳。这个端点让第二个实例能判断：

| 探测结果 | 含义 | 动作 |
|---|---|---|
| `/api/ping` 返回我们的 `app` 且 `source` 相同 | 已有同款实例 | **打开浏览器指向它，然后退出 0** |
| `/api/ping` 返回我们的 `app` 但 `source` 不同 | 另一个切换器（Trae 侧）在用这个端口 | 换端口，但**自己仍受互斥体约束** |
| 端口有响应但不是我们的 `app` | 被别的程序占用 | **明确报错**，提示换端口，**不顺延** |
| 端口无响应 | 空闲 | 正常启动 |

> 顺延端口这件事本身要重新审视：当前是**无条件顺延**，用户根本不知道
> 自己打开的是第几个实例。改成"只在确认端口被**非自己**的程序占用时才顺延，
> 且必须打印实际端口"。

### 1.3 组装起来的启动流程

```
main()
  │
  ├─ 1. 拿命名互斥体
  │      already_running?
  │        ├─ 是 → 探测默认端口的 /api/ping
  │        │        ├─ 是我们的实例 → 打开浏览器指向它 → 退出 0
  │        │        └─ 不是       → 打印"已有实例在跑但端口被占，请先关掉它" → 退出 1
  │        └─ 否 → 继续
  │
  ├─ 2. 探测目标端口
  │      ├─ 空闲                → bind
  │      ├─ 是我们的实例（异常情况：互斥体拿到了但服务还在）
  │      │                      → 复用，不再 bind
  │      └─ 别人的程序          → 顺延并在**显眼位置**打印实际端口
  │
  ├─ 3. 起服务 / 开窗口
  │
  └─ 4. 退出时释放互斥体（CloseHandle；进程结束内核也会释放）
```

### 1.4 迁移必须独占，不只是"客户端不在跑"

迁移的前置检查目前只查客户端进程。还要加一条：**确认没有第二个切换器实例**。

因为迁移会改 db 与目录，而另一个实例可能同时在切号/续期。虽然
`.locks/wb-desktop.lock` 能串行化，但两个实例的内存态是各自独立的 ——
另一个实例的"当前账号"缓存会在迁移后失效，继续操作就可能覆盖迁移结果。

→ 迁移前在 `migrate()` 的前置检查里加：**探测默认端口的 `/api/ping`，
若返回的 `pid` 不是自己 → 拒绝迁移**。

### 1.5 顺带要修的 `.cmd`

```bat
where python >nul 2>nul
if errorlevel 1 ( echo [ERROR] python not found. ... )
python wb_ui_server.py --serve
```

问题：

1. **`where python` 只检查"存在"，不检查"可用"**。本机 `where python` 会列出
   两个：`C:\Program Files\Python313\python.exe` 和
   `...\WindowsApps\python.exe`（Microsoft Store 占位符）。前者在前所以能用，
   但若哪天 PATH 顺序变了，`python` 会打开应用商店 —— 表现为"双击没反应"。
   建议改成显式校验：`python -c "import sys;assert sys.version_info>=(3,8)"`。
2. **不打印实际端口**。顺延后用户不知道页面在哪。
3. **没有单实例提示**。第二个实例应该明确说"已在运行（pid xxx，http://127.0.0.1:8765）"。

---

## 2. 需要验证的场景

| # | 场景 | 期望行为 | 怎么验 |
|---|---|---|---|
| 1 | 双击 .cmd 两次（间隔 <1s） | 第二个退出 0，浏览器指向第一个 | 两个 cmd 窗口，只有一个服务在 LISTEN |
| 2 | 双击 .cmd 两次（间隔 5s） | 同上 | 同上 |
| 3 | 先 .cmd，再双击 exe | 同上（两者共用同一互斥体名） | 同上 |
| 4 | 先 exe，再 .cmd | 同上 | 同上 |
| 5 | 第一个实例**正常关闭**后再次启动 | 正常启动 | 关闭窗口 → 再双击 → 应能起 |
| 6 | 第一个实例**被任务管理器强杀**后再次启动 | 正常启动（内核已释放互斥体） | 强杀 → 再双击 → 应能起 |
| 7 | 第一个实例**崩溃**（未捕获异常）后再次启动 | 正常启动 | 同上 |
| 8 | 默认端口被**别的程序**占用 | 顺延，且**显眼打印实际端口**；仍只有一个自己的实例 | 用 `python -m http.server 8765` 占住，再启动 |
| 9 | 默认端口被**旧版本的自己**占用 | 提示"已有实例在跑（pid xxx）"，复用而不是另起 | 用旧代码起一个，再用新代码启动 |
| 10 | 同时启 `wb_ui_server.py`（wb 侧）与 `tw_ui_server.py`（Trae 侧） | **两个都要能跑**（不同 `source`，不同默认端口） | 同时启动，确认 8765 + 8766 都在服务 |
| 11 | 两个不同 Windows 登录会话各启一个 | 都能跑（`Local\` 语义） | RDP 双会话测试；若改用 `Global\` 则第二个应被拒 |
| 12 | 无权限创建互斥体（受限账户） | **降级放行**并打印警告，不阻断启动 | 低权限账户运行 |
| 13 | 迁移进行中，第二个实例启动 | 第二个应被拒或复用；**不得干扰迁移** | 迁移中双击 .cmd |
| 14 | 互斥体名与其它软件冲突 | 不会（用 `Local\WorkBuddySwitcher` 这种带前缀的名字） | 检查命名规范 |
| 15 | 服务已起但窗口/浏览器关掉 | 服务仍在（当前行为）；再次双击应复用而非另起 | 关掉浏览器标签，再双击 .cmd |

### 需要额外注意的两点

- **场景 10 是硬约束**：wb 与 tw 是两个不同工具，默认端口就不同（8765 / 8766），
  互斥体名必须带 `source`，否则会互相把对方挤掉。
- **场景 6/7 是选互斥体而不是 PID 文件的主要理由**，必须实测，不能只看文档。

---

## 3. 落地改动清单

| # | 改动 | 位置 | 说明 |
|---|---|---|---|
| 1 | `acquire_single_instance(name)` | `switcher_common.py` | ctypes + `CreateMutexW`，返回 `(handle, already)`；非 Windows 返回 `(None, False)` |
| 2 | `probe_instance(port)` | `switcher_common.py` | 打 `/api/ping`，区分"我们的实例 / 别人 / 空闲" |
| 3 | `GET /api/ping` | `BaseHandler.api_get` | 返回 `app / source / pid / port / version` |
| 4 | 启动流程接入 | `wb_ui_server.py --serve`、`wb_ui_app.py`、`tw_*` | 见 §1.3；互斥体句柄存到模块级变量防止被 GC 回收 |
| 5 | 迁移前置检查加"无第二实例" | `account_migration.migrate` | 探测默认端口，`pid != 自己` 则拒绝 |
| 6 | `.cmd` 三处改进 | `workbuddy_switcher.cmd` / `trae_switcher.cmd` | 校验 Python 可用性、打印实际端口、单实例提示 |
| 7 | 回归断言 | `smoke_test.py` | 见下 |

### 回归自检要覆盖的（不依赖真的起两个进程）

- `acquire_single_instance()` 在同一进程内**两次调用**：第一次 `already=False`，
  第二次 `already=True`（同名互斥体在同一进程内也会置 183 —— 注意这与
  "两次 `CreateMutexW` 返回同一句柄"是同一件事，要断言清楚）。
- `probe_instance(port)` 对"空闲端口 / 我们的服务 / 非我们的服务"三种情况分别返回
  正确判定。用一个临时起的 HTTP 服务冒充"非我们的服务"。
- `/api/ping` 返回体包含 `app` / `source` / `pid` / `port`。
- `migrate()` 在"探测到另一个实例"时拒绝，且**不创建任何备份目录**。
- 模板与 `--serve` 的帮助文本里提到实际端口。

---

## 4. 与迁移的关系

单实例保护不是锦上添花 —— **它是迁移正确性的前提之一**：

```
迁移的完整前置条件
├─ 1. 客户端（WorkBuddyAI.exe 等）未运行        ← 已实现
├─ 2. 没有第二个切换器实例在跑                   ← 本文档要补的
└─ 3. 没有其它迁移在进行（.migration-backup 无残留）← 已实现（重入检测）
```

缺第 2 条时，另一个实例可能在你迁移到一半时切号，把它的内存态写回
`workbuddy-desktop.info`，或者在旧 uid 下新建会话 —— 迁移结果就不完整了。
