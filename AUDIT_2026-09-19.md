# 全量功能验证报告

> 验证时间：2026-09-19 00:58 ~ 01:15
> 代码版本：`ed4f41e`（工作区干净）
> 验证方式：自动化基线 + 独立端到端探针 + 真实运行时日志分析
> **本报告只做验证与取证，未修改任何代码。**

---

## 一、验证方法

| 手段 | 覆盖 |
|---|---|
| `smoke_test.py` | 267 条断言（含 18 段：裁剪/锁/端口/接口/令牌/切号落盘/迁移/单实例/去重/布局） |
| 独立端到端探针 | 起一个不带守卫的服务，逐个打 16 个接口 + 22 项前端元素 |
| Trae 侧探针 | 同上，验证 4 个 wb 专有接口在 tw 侧正确 404 |
| 桌面版 exe | `TraeSwitcher.exe --serve` 端到端（WorkBuddy exe 因单实例保护无法独立启动） |
| 真实日志分析 | `logs/switcher.log` 1151 行，按动作/原因/日期统计 |
| 静态排查 | 全项目搜未保护的 `.stat()` / `read_text` / 写操作 |

---

## 二、功能验证结果

### ✅ 正常（逐项）

| # | 功能 | 结果 |
|---|---|---|
| 1 | 账号列表 `/api/accounts` | 7 个账号，字段齐全（file/ok/nickname/uid/token_source） |
| 2 | 当前账号 `/api/current` | 正常；**正确排除** `workbuddy-desktop-ai.*` |
| 3 | 积分明细 `/api/credits` | queried=7；第二次命中缓存（0 重复回源） |
| 4 | 签到状态 `/api/checkin-status` | 7 个账号并发查询 |
| 5 | 一键签到 `/api/checkin` | **7/7 成功**，1504ms |
| 6 | 单账号续期 `/api/refresh` | 330ms 成功 |
| 7 | 一键续期 `/api/refresh-all` | **7/7 成功**，3107ms |
| 8 | 切换账号 `/api/switch` | 113ms；切后 uid 正确变化；切回成功 |
| 9 | 迁移预览 `/api/migrate-preview` | needed=True，6 项待迁、2 条冲突提示 |
| 10 | 打开客户端 `/api/open-client` | 识别"已在运行"，**不重复启动**（11 → 11） |
| 11 | 客户端状态 `/api/client-status` | 可 JSON 序列化 |
| 12 | 新增账号 `/api/add` | 成功；重复/无效内容正确拒绝并回滚 |
| 13 | 删除账号 `/api/remove` | 成功；不存在的正确拒绝 |
| 14 | 身份端点 `/api/ping` | 字段齐全（app/source/pid/port/version/page_open） |
| 15 | 页面心跳 `/api/page-alive` | 免令牌可访问 |
| 16 | 单实例保护 | 空端口 `action=start`；有实例 `reuse`；exe 二次启动正确复用 |
| 17 | 页面去重 | `page_open` 正确演变；复用分支不再开新页面 |
| 18 | 安全：令牌 | 无/错令牌 → 401；只读端点免令牌 |
| 19 | 安全：方法 | 写接口 GET → 405 |
| 20 | 安全：同源 | Host 非回环 → 403 |
| 21 | 安全：路径穿越 | `../../` 与 `notinfo.txt` 均被拒 |
| 22 | 前端 22 项元素 | 21 项在位（导航栏/锚点/工具条/迁移弹框/心跳/滚动高亮…） |
| 23 | Trae 侧 | ping/accounts/current 正常；4 个 wb 专有接口正确 404 |
| 24 | Trae 侧模板 | `MIGRATE` 关闭、签到按钮隐藏、无占位符残留 |
| 25 | 桌面版 exe | 启动/服务/接口/单实例复用全部正常；webview 已打包 |
| 26 | 辅助脚本 | `check_ttl.py`、`import_token.py` 正常 |
| 27 | 启动脚本 | 4 个 `.cmd` 调用目标正确 |
| 28 | 审计日志 | 1151 行，格式异常 0 |
| 29 | 计划任务 | `WB-Switcher-RefreshAll` Ready，上次 07:30 result=0，下次 09/19 07:30 |

**自动化基线：267 / 267 PASS。**

---

## 三、发现的 BUG

### 🔴 BUG A（功能性，仍在发生）：`current_account()` 的 `f.stat()` 未做异常保护

**位置**：`wb_ui_server.py:167`

```python
entries.append({
    ...
    "mtime": f.stat().st_mtime,     # ← 唯一没有保护的文件操作
})
```

**问题**：`current_account()` 会遍历桌面端 auth 目录下全部 `workbuddy-desktop*.info`（本机 11 个），
对每个文件调 `stat()`。当某个文件正被杀软/索引器/客户端 watcher 以独占方式持有句柄时，
`stat()` 抛 `PermissionError: [WinError 5] 拒绝访问`，**异常一路冒到 HTTP 层 → 500**。

同一个函数里另外两处读取**都有保护**，只有这一处没有：

| 操作 | 保护 |
|---|---|
| `wb.session_from_info_file(f)` | ✅ 上游自带 `except (OSError, ValueError)` |
| `_read_info(f)` | ✅ 自带 `except (OSError, ValueError)` |
| **`f.stat().st_mtime`** | ❌ **无** |

**证据（真实日志，11 次，跨 2 天持续发生）**：

```
2026-09-18 09:25:55 [wb] switch FAIL target=workbuddy-account-c.info
    服务器错误（PermissionError）：[WinError 5] 拒绝访问。: '...\auth\workbuddy-desktop.info'
...
2026-09-19 01:01:27 [wb] switch FAIL target=workbuddy-hlqin.info
    服务器错误（PermissionError）：[WinError 5] 拒绝访问。: '...\auth\workbuddy-desktop.info'
```

时间范围 `09-18 09:25:55 ~ 09-19 01:01:27` —— **不是历史问题，现在仍在发生**。

**影响范围**：
- `POST /api/switch`：偶发 500。**好在异常发生在动任何文件之前**（`current_uid()` 在加锁前调用），
  所以**不会损坏数据**，但用户看到"服务器错误"会以为切号失败而重试。
- `GET /api/current`：偶发 500 → 页面显示"无法获取桌面端当前账号"（GET 不写审计，所以日志里看不到）。
- 频率：2 天 11 次，约 0.5 次/天；每次概率与杀软扫描时机相关。

**复现方式**：不易稳定复现（依赖 AV 持有句柄的时机）。我试过并发替换 + 高频 stat（26578 次）未能触发 ——
本机杀软不介入临时目录。**但代码层面可以确定**：它是全项目唯一未保护的 `.stat()`（已全量搜索确认）。

**建议修法**（待确认，见第四节）

---

### 🟡 BUG B（轻微，仅影响审计可读性）：令牌校验失败的审计动作名写成路径

**位置**：`switcher_common.py:1018`

```python
audit(self.AUDIT_DIR, self.SOURCE, u.path, "", False, "令牌校验失败")
#                                ^^^^^^ 应为 self.AUDIT_ACTIONS.get(u.path, u.path)
```

**问题**：`do_POST` 里其余地方的审计都用 `self.AUDIT_ACTIONS.get(u.path, u.path)` 做映射，
只有令牌校验这一条直接传了 `u.path`。于是日志里出现：

```
2026-09-19 01:00:08 [wb] /api/switch FAIL target=-   令牌校验失败
```

而正常的切号记录是 `[wb] switch`。

**影响范围**：仅审计日志的可读性与可统计性 —— 按动作名统计时 `/api/switch` 与 `switch` 会分成两组。
**不影响任何功能**。已发生 7 次。

**复现方式**：带错误令牌 POST `/api/switch`，然后看 `logs/switcher.log`。

---

## 四、一致性问题与优化建议（**待确认后再执行**）

### 建议 1：给 `current_account()` 的 `stat()` 加保护（对应 BUG A）

**方案**：把每个文件的处理整体包进 `try/except OSError`，失败的条目**跳过**（而不是让整张表失败）。

```python
for f in DESKTOP_DIR.glob("*.info"):
    try:
        root_id, is_backup = wb.split_info_name(f.name)
        ...
        mtime = f.stat().st_mtime
    except OSError:
        continue          # 文件正被占用：这次先跳过，下次刷新就正常
    entries.append({...})
```

**可选加强**：加一个 `_stat_with_retry(f, tries=3)`（与 `account_migration._replace` 同样的思路，
Windows 上刚写完的文件被杀软短暂持有，等几十毫秒就过去了）。

**风险**：极低。最坏情况是某个文件这一轮没被列出来，下一轮就有。
**影响**：切号与 `/api/current` 不再因杀软扫描偶发 500。

---

### 建议 2：修掉 BUG B 的审计动作名

**方案**：一行改动，`u.path` → `self.AUDIT_ACTIONS.get(u.path, u.path)`。

**风险**：无。**影响**：日志可按动作名正确聚合。

---

### 建议 3：统一 `checkin_snapshot` 与 `credits_snapshot` 的返回字段

**现状**：

| 快照 | 字段 |
|---|---|
| `credits_snapshot` | `accounts, cached, ok, **queried**, total, total_remain, ts` |
| `checkin_snapshot` | `accounts, cached, ok, **checked**, total, ts` |

两者是姊妹接口，但一个叫 `queried`（成功查询数）、一个叫 `checked`（**已签到**数，语义不同），
且 checkin 侧**没有**"成功查询数"。

**影响**：**当前无功能影响**（前端只用 `accounts` 和 `ts`；自检也没断言 checkin 的 `queried`）。
属于 API 一致性问题 —— 将来若有人按 credits 的写法去用 checkin，会踩空。

**方案**：给 checkin 补一个 `queried`（成功查询数），保留 `checked`。或统一命名。
**风险**：低（纯增字段）。**影响**：接口更一致，便于前端将来展示"7/7 查询成功"。

---

### 建议 4：给 `switch_account` 的文件操作加重试

**背景**：`account_migration._replace()` 已经有重试（6 次递增退避），因为迁移时实测遇到过
`WinError 5`；但 `switch_account` 里的 3 处文件操作**没有**：

| 操作 | 现状 |
|---|---|
| `DESKTOP_INFO.replace(backup)` | `except OSError` → 返回友好提示（不 500） |
| `tmp.replace(DESKTOP_INFO)` | `except OSError` → 回滚 + 友好提示（不 500） |
| `marker_path.unlink()` | `except OSError` → 忽略 |

**这些都不会 500**，但会让切号**直接失败**（用户看到"轮换旧登录态失败"）。
日志里目前没有这类失败（只有 BUG A 的 500），所以**属于预防性优化**，不是现存 BUG。

**方案**：把 `account_migration._replace` 提到 `switcher_common` 里共用，`switch_account` 改用它。
**风险**：低。**影响**：切号在杀软扫描窗口内更稳。

---

### 建议 5：exe 同级没有账号目录时给出更明确的引导

**现象**：`TraeSwitcher.exe --serve` 单独跑时 `/api/accounts` 返回 0 个 —— 因为
`tw_auth/` 不在 exe 同级。前端**已有**空态提示（"账号库…目录下没有账号文件"），
但没告诉用户"要把账号目录放到 exe 旁边"。

**方案**：空态文案补一句部署约定（wb 侧 `{{EMPTY_HINT}}` 已有类似机制，可复用）。
**风险**：无。**影响**：用户双击 exe 时不会困惑。

---

### 建议 6（低优先）：`preview()` 与 `migrate_preview()` 的职责边界

`account_migration.preview()` 不含 `needed` 字段，`needed` 由服务层 `wb_ui_server.migrate_preview()` 补。
两个函数名相近、返回结构不同，容易误用（我自己在验证时就先调错了一次）。

**方案**：把 `needed` 下沉到 `preview()`，或把服务层那个改名为 `preview_for_ui()`。
**风险**：低。**影响**：API 更自洽。

---

## 五、未能验证的部分（诚实说明）

| 项 | 原因 |
|---|---|
| WorkBuddy 桌面版 exe 的独立启动 | 当时 8765 上的实例持有单实例锁，exe 正确地拒绝了启动（**预期行为**）。**后续已补齐**：用 `--serve --port <冷门端口>` 绕开单实例锁完成端到端验证，并固化成 `check_exe_datadir.py`（双探针法，见 README 的「exe 数据目录基准」） |
| **「迁移前自动关闭客户端」的实际执行** | `WorkBuddyAI.exe` **就是托管本对话的客户端**，真去关它会直接中断会话。已用 mock 覆盖 4 个分支（没在跑/枚举失败/关不掉/关得掉），但**没有真实跑过一次** |
| 真实迁移（移动用户数据） | 会动你的真实对话数据。自检用临时目录完整覆盖了迁移+回滚，但没有对真实账号执行 |
| 桌面版原生窗口 | 当时需要打开窗口（会弹到桌面上），未做。**后续已补齐**：无参数启动 exe，`tasklist /V` 确认窗口标题存在、进程存活、`logs\desktop-start.log` 无「WebView 启动失败」——即 pywebview 正常加载，没有悄悄退回浏览器 |
| Trae 侧素材续期 | 日志里有 3 条 `HTTP 401`，属已知的 Trae 遗留问题（你已决定暂不处理） |

---

## 六、结论

- **功能整体健康**：29 项功能全部按预期工作，自动化基线 267/267 PASS。
- **1 个功能性 BUG（BUG A）**：`current_account()` 的 `f.stat()` 未保护 → 切号与 `/api/current` 偶发 500，
  **2 天 11 次，仍在发生**。异常发生在动文件之前，**不损坏数据**。
- **1 个轻微 BUG（BUG B）**：令牌校验失败的审计动作名写成路径，仅影响日志统计。
- **4 项一致性问题/预防性优化**（建议 3~6），无现存功能影响。

**以上 6 条建议都还没有动手** —— 等你确认要做哪几条。


---

# 修复完成记录（2026-09-19 01:50）

> 用户确认「完成所有任务，包括 BUG A 和 BUG B」。以下 6 项**全部已实施并逐项验证**。
> 自检从 **267 → 287 项全 PASS**（新增第 19 段 20 条回归断言），连跑两次稳定。

| # | 项目 | 状态 | 验证方式 | 结果 |
|---|---|---|---|---|
| A | `current_account()` 的 `stat()` 未保护 | ✅ 已修 | monkeypatch `Path.stat` 抛 `PermissionError` | 7/7 PASS |
| B | 令牌校验失败的审计动作名 | ✅ 已修 | 带错令牌 POST，检查审计内容 | 2/2 PASS |
| 3 | 统一 checkin / credits 字段 | ✅ 已做 | 比对两个快照的字段与语义 | 5/5 PASS |
| 4 | 改名重试统一到 `common` | ✅ 已做 | 注入失败验证重试 + 切号端到端 | 7/7 PASS |
| 5 | exe 空态文案补部署约定 | ✅ 已做 | 渲染页面检查文案 | 2/2 PASS |
| 6 | `needed` 只保留一个计算点 | ✅ 已做 | 直接调 + HTTP 调，比对取值 | 8/8 PASS |

## 各项的具体改法

### BUG A — `wb_ui_server.py`

新增 `_mtime_with_retry(path, tries=3, delay=0.05)`；`current_account()` 的循环体整体包进
`try/except OSError`，**单个文件读不到只跳过它，不再冒到 HTTP 层**。

**实施中发现并修正了我自己引入的副作用**：第一版是"stat 不到就 `continue`"，
结果**正式文件被跳过、一个旧备份顶上来当了"当前账号"**（条目数 11 → 10，current 变成了备份）。
那会显示成别人的账号 —— 比 500 更糟。改成给兜底 mtime：

```python
mtime = time.time() if f.name == DESKTOP_ROOT_ID + ".info" else 0.0
```

正式文件拿"现在"（必然大于所有已有文件）保持第一，备份拿 0 排到最后。
**没有用 `float("inf")`** —— mtime 会进 JSON，`Infinity` 不是合法 JSON，浏览器 `JSON.parse` 会直接抛错。

### BUG B — `switcher_common.py`

```python
audit(self.AUDIT_DIR, self.SOURCE,
      self.AUDIT_ACTIONS.get(u.path, u.path), "", False, "令牌校验失败")
```

### 建议 3 — `wb_ui_server.py`

`checkin_snapshot` 补 `queried`（=成功查询数），与 `credits_snapshot` 对齐；
`checked`（=今日已签到数）语义不同，保留。

### 建议 4 — `switcher_common.py` / `account_migration.py` / `wb_ui_server.py` / `tw_ui_server.py`

新增 `common.replace_with_retry()` 作为**唯一实现**；`account_migration._replace` 改为委托；
`switch_account` 的 2 处改名 + `_restore_backup`、以及 tw 侧的 `tmp.replace(sp)` 全部改用它。

### 建议 5 — 两个 server 的 `EMPTY_HINT`

补一句"直接运行 exe 时，账号目录要放在 exe 同级（用 .cmd 启动就是本目录）"。

### 建议 6 — `account_migration.py` / `wb_ui_server.py`

`needed` 下沉到 `preview()`，`migrate_preview()` 不再重算（同一值算两遍，改一处忘一处就会不一致）。

## 仍未覆盖（与验证阶段相同，非本次修复范围）

- 「迁移前自动关闭客户端」的真实执行 —— `WorkBuddyAI.exe` 就是托管本对话的客户端，真关会中断会话（仅 mock 覆盖）
- 真实迁移（会动真实数据）
- 桌面版原生窗口
- Trae 侧素材续期（已知遗留问题，用户已决定暂不处理）
