# 全功能模块审计报告

**日期**：2026-09-24
**对象**：wb_switcher（WorkBuddy 双通道账号切换器 + Trae 姊妹工具），15,608 行
**方式**：逐模块**静态精读 + 实证核验**（贴代码原文、`hasattr`/计时/对拍验证），
分 5 路并行审查后由主线逐条复核
**基线对照**：`smoke_test.py` **508 PASS / 0 FAIL**，退出码 0
**核验原则**：代理提出的每条结论都回源码复看；凡与既有约定冲突或无法复现的，一律**降级或撤销**（见第八章）

---

## 一、结论速览

| # | 模块 | 最高严重度 | 问题数 | 状态 |
|---|---|---|---|---|
| 1 | 数据迁移 `account_migration` | **P0** | 3 | ❌ 有真实缺陷 |
| 2 | 账号切换 `switch_account` | **P1** | 2 | ❌ 有真实缺陷 |
| 3 | 锁与并发（跨模块） | **P1** | 2 | ❌ 有真实缺陷 |
| 4 | Trae 侧切换 / 续期 | **P1** | 1 | ⚠️ 部分属用户已决定暂不处理 |
| 5 | 前端两份模板 | **P1** | 5 | ⚠️ 有真实缺陷 |
| 6 | HTTP 骨架与安全守卫 | P2 | 6 | ⚠️ 有真实缺陷 |
| 7 | 内存凭据 `live_token` | P2 | 2 | ⚠️ 有真实缺陷 |
| 8 | 窗口守卫 `window_state_guard` | P2 | 1 | ⚠️ 有真实缺陷 |
| 9 | 辅助脚本 / 打包 | P2 | 5 | ⚠️ 有真实缺陷 |
| 10 | 缓存与热路径性能 | P2 | 3 | ⚠️ 有真实缺陷 |
| 11 | 启动器 / 可维护性 | P3 | 9 | 提示 |

**总计**：**P0 × 1、P1 × 9、P2 × 17、P3 × 12**。
其中 **1 项 P0 与 4 项 P1 建议立即修**（迁移数据面 + 客户端被关掉不回来），其余按需。

---

## 二、P0 —— 建议立即修

### P0-1 迁移 storage 合并分支必然抛 `AttributeError`

**位置**：`account_migration.py:616`

```python
if dst.exists():
    _merge_storage_dir(d, dst)
    moved.append("%s → %s（合并）" % (d.name, new_name))
    common.safe_unlink_tree(d)        # ← 这个函数全仓不存在
```

**实证**：

```
hasattr(common,"safe_unlink_tree") = False
hasattr(common,"safe_rmtree")      = True
```

`switcher_common.py` 只定义了 `safe_unlink`（单文件）与 `safe_rmtree`（目录），
全仓 `safe_unlink_tree` 仅此一处引用，也没有模块级 `__getattr__` 兜底。

**触发条件**：切号时**新账号已存在同名 `storage/user-<uid>-*` 目录** ——
来回切号（A→B→A）必然命中，属常规操作而非边角情况。

**后果链（比异常本身更糟）**：

1. `_merge_storage_dir(d, dst)` **已经执行完** → 旧目录内容已复制进新目录；
2. 紧接着 `safe_unlink_tree(d)` 抛 `AttributeError`；
3. 被 622 行 `except` 捕获 → `_rollback(...)` → 报「迁移 storage 失败」；
4. 而 `_rollback` **不还原 storage 目录**（见 P1-1）→ 旧目录还在、新目录多了内容。

净结果：**数据面「两处都有」（账号级设置重复），用户看到的是「迁移失败」**。
这是本次审计唯一的 P0。

**改进**：

```python
common.safe_rmtree(d)          # 语义一致（目录删除，走回收站，含 BaseException 兜底）
```

并补一条 smoke 断言：`not hasattr(common, "safe_unlink_tree")` 不该被引用，
或直接断言 `account_migration.py` 里不出现 `safe_unlink_tree`。

---

## 三、P1 —— 功能错误 / 数据面不一致

### P1-1 迁移回滚不消费目录快照，合并分支无法还原

**位置**：`account_migration.py:543-549`（快照）vs `:767-791`（回滚）

```python
# 快照阶段（545 / 548）
for d in (r / "storage").glob("user-%s-*" % _safe(old_uid)):
    _copy_tree(d, journal.backup / "storage" / d.name)
cdir = r / "connectors" / _safe(old_uid)
if cdir.is_dir():
    _copy_tree(cdir, journal.backup / "connectors" / cdir.name)
```

回滚只处理 `journal.renames`（`reversed`）、`journal.restore`、db 备份三类，
**`journal.backup/storage/` 与 `journal.backup/connectors/` 从未被读回**。

**影响**：`storage` 的「合并」分支（旧目录被删、文件并入新目录）**不写 `journal.renames`**
→ 一旦后续步骤失败回滚，被删的旧目录永久消失、并入的内容留在新目录。
P0-1 触发的正是这条路径，两者叠加放大。

**改进**：二选一 —— ① 合并分支把「旧目录 → 备份目录」也写进 `journal.renames`
（`_copy_tree` 后 `journal.renames.append((d, journal.backup / "storage" / d.name))`）；
② `_rollback` 增加从目录快照 `_copy_tree` 还原的分支。建议 ①（复用已有机制，改动最小）。

---

### P1-2 manifest 只在全部成功后落盘，与文件头承诺矛盾

**位置**：`account_migration.py:16`（声明）vs `:731-734`（实现）

```python
# 文件头
  2. 每一步都记进 manifest，失败可按 manifest 反向回滚；
# 实际
    # ---- 9. 落盘 manifest ----
    (journal.backup / "manifest.json").write_text(...)      # 只在第 9 步，全部成功之后
```

**影响**：进程被杀 / 断电 / 用户强杀时，磁盘上**没有任何恢复依据** ——
留下部分改写的账号数据，且没有现场说明，比「失败并回滚」更难排查。
（正常失败靠内存 journal 回滚尚可，所以这条只在异常终止时暴露。）

**改进**：关键步（文件快照后、db 改写后、storage/connectors 改写后）增量落盘 manifest；
或至少在第 4 步（快照完成）后先写一份。

---

### P1-3 切号失败时客户端被关掉**不再拉起**

**位置**：`wb_ui_server.py:994-1049`（4 条早退路径）vs `:1091-1095`（唯一重开点）

```python
if migrate and migrate.get("enabled", True) and migrate.get("close_client", True):
    ok_c, msg_c, client_was_running = close_client(ch)      # ← 先关了客户端
    ...
except OSError as e:
    return False, "轮换旧登录态失败：%s" % common.scrub(e), None          # 1013 早退
...
    return False, "写入新登录态失败：%s%s" % (...), None                  # 1028 早退
...
    return False, ("写入后校验未通过：…"), None                            # 1048 早退
...
# 函数末尾（只有走完全程才会到达）
# 恢复客户端原状：原来在跑就重新打开（迁移成功时它会直接读新账号的登录态）。
# 无论迁移成败都恢复 —— 是我们把它关掉的，就该由我们把它放回去。
if client_was_running and migrate and migrate.get("reopen_client", True):
    ok_o, msg_o = open_client(ch)
```

**注释自己写着「无论迁移成败都恢复」，但代码结构决定了 4 条失败路径全部走不到它。**

**影响**：
- 客户端被静默关掉，用户得自己手动开回来；
- 第 4 条（写后校验未通过）时**桌面端其实已经是新账号**，却回 `ok=False`，
  用户看到「失败」而实际状态已变 —— 与 09-21 修过的 P1-1 是同一类「报告与状态相反」。

**改进**：`close_client` 之后的主体包 `try/finally`：

```python
try:
    ...  # 原有全部逻辑
finally:
    if client_was_running and migrate and migrate.get("reopen_client", True):
        open_client(ch)
```

同时把「写后校验未通过」的文案改成中性表述（「可能未生效，请确认」而非「失败」）。

---

### P1-4 切号锁与续期/签到/删除锁**不是同一把**

**位置**：`wb_ui_server.py:978` vs `:1133 / :1167 / :1259 / :1454`

```python
# 切号
with common.file_lock(ch.lock_key, LOCK_DIR, timeout=lock_timeout):        # "wb-desktop"
# 续期 / 签到 / 删除 / 新增
with common.file_lock(ch.key + "-auth-" + base, LOCK_DIR):                 # "wb-auth-xxx.info"
```

切号注释宣称「与续期/另一次切号交叉会写坏登录态」所以加锁，但续期侧用的是另一把 key。

**影响**：切号正在读 `src` 的同时，续期可写同一个 `.info`；`remove_account` 可删掉切号正要用的源文件。
虽然写盘走 tmp + 原子替换（不会读到半截 JSON），但会读到**刷新前/刷新后任意一版**，
且源文件可能在读取后消失。

**改进**：定义单一入口 `ch.account_lock(base)`（如 `ch.key + "-auth-" + norm(base)`），
切号对目标账号也加这把锁；或反过来把续期/签到/删除改为也加 `ch.lock_key`。
**不建议两把都加**（顺序不当会死锁）。

---

### P1-5 Trae 设备键只增不删，切换后设备凭据与登录态不匹配

**位置**：`tw_ui_server.py:363-364`（收集）vs `:402-404`（写入）

```python
target_dc = {str(k): v for k, v in data.items()
             if str(k).startswith("iCubeAuthInfo://icube-dc")}
...
cur[AUTH_KEY] = target_enc
for k, v in target_dc.items():
    cur[k] = v          # ← 只覆盖素材里有的键；本机旧账号的同类键原样残留
```

注释写「切换时一并应用」，但没有先清掉 `cur` 里本机的旧 `icube-dc*` 键；
素材缺该键时更是只换了 cloudide 密文。

**影响**：设备凭据与登录态不匹配，Trae 客户端可能拒绝/丢弃新登录态 ——
与用户已反馈的 `20403 Token device not match` 是同一族问题（**根因不同，别混为一谈**）。

**改进**：写入前先删 `cur` 中所有 `iCubeAuthInfo::/icube-dc*` 键再写目标键；
素材完全没有设备键时明确报错，而不是静默只换一半。

---

### P1-6 `currentMatches` 漏判 `c.live`，切号后轮询全部落空

**位置**：`ui_template.html:1036-1040`

```js
function currentMatches(nick){
  const c = lastCurrent && lastCurrent[0];
  if(!c) return false;
  return !!nick && c.ok!==false && c.nickname===nick;    // ← 漏了 || c.live
}
```

`renderCurrent()`（`:784`）与 `ui_hub.loadCurrent()`（`ui_hub.html:257`）**都已修成**
「`ok===false && live` 视为正常」，这里没跟上 —— 典型的三处同类判据漂移。

**影响**：客户端 5.6.2+ 静态字段保护下 `ok` 恒为 `false`，切号成功后
`settleAfterSwitch`（`:1041`）6 次轮询全部判不匹配 → 白打约 12 个请求，且**不给任何提示**，
用户以为切号没生效。

**改进**：`return !!nick && (c.ok!==false || c.live) && c.nickname===nick;`
并在 smoke 里钉住「三处判据必须同源」（可 grep 断言三处都出现 `c.live`）。

---

## 四、P2 —— 性能 / 稳健性 / 安全加固

### P2-1 `_params` 无体积上限，非法 `Content-Length` 可挂死工作线程

**位置**：`switcher_common.py:1205-1208`

```python
length = int(self.headers.get("Content-Length") or 0)   # 非数字 → ValueError → 500
if not length:
    return data
raw = self.rfile.read(length).decode("utf-8", "replace")  # 负值 → read(-5) 读到 EOF 挂住
```

**影响**：本机页面正常，但任何能连到 87xx 的本地进程都可触发；
`ThreadingHTTPServer` 下会占住一个线程直到连接关闭，且**无上限可撑爆内存**。

**改进**：

```python
try:
    length = int(self.headers.get("Content-Length") or 0)
except ValueError:
    raise ValueError("Content-Length 非法")
if length < 0 or length > MAX_BODY:        # MAX_BODY = 1 << 20
    raise ValueError("请求体过大")
```

---

### P2-2 页面路由位于 `_guard()` 之前，令牌随页面外泄（纵深防御不一致）

**位置**：`switcher_common.py:1263-1269`（路由）vs `:1289`（`_guard()`）

```python
if u.path == "/":            self._serve_index();  return
if u.path in self.PAGES:     self._serve_page(...); return
...
if not self._guard():        return
```

`_serve_page` 会注入 `window.SWITCHER_TOKEN`（`:1362-1365`），而 `_guard()`
（`:1196`）= `same_origin(headers)`，**含 Host 回环校验**。这两个页面分支绕过了它。

> **核验降级说明**：代理把它定为 P1「令牌泄漏可被利用」，我复看后**降为 P2**。
> 理由：`do_POST` 里 `_guard()`（Host + Origin）与令牌校验**是串联的两道**，
> 攻击者即使通过 DNS rebinding 读到令牌，POST 仍会因 `Host: evil.com` 被 403。
> 因此这是**纵深防御不一致 + 与 README 声明不符**，不是可直接利用的漏洞。

**改进**：把两个页面分支移到 `_guard()` 之后；若担心「用户手动输地址打开页面」被挡，
可对页面路由只做 Host 校验、跳过 Origin 校验（加一个 `_guard(origin=False)` 参数）。

---

### P2-3 `live_token` 缓存命中分支忽略 `want_refresh`，诊断结果随调用顺序漂移

**位置**：`live_token.py:326-336`（缓存）vs `:345`（`stop` 选择）

```python
if hit and now - hit["ts"] < CACHE_TTL_SECONDS:
    sessions = hit.get("sessions") or {}
    if expect_uid:
        return sessions.get(expect_uid)
    best = hit.get("best_uid")
    return sessions.get(best) if best else None        # ← 不看 want_refresh
...
stop = _stop_on_both if want_refresh else _stop_on_access
```

**影响**：60 秒内若先有功能路径调用（`want_refresh=False`，`refresh_token` 为空），
随后 `/api/live-token` 传 `want_refresh=True` 会命中同一份缓存，
**拿到空 `refresh_token`**，诊断结论「该账号没有 refreshToken」是假的。

**改进**：缓存键并入 `want_refresh`（`_CACHE[f"{key}|{int(want_refresh)}"]`），
或 `want_refresh=True` 时强制不读缓存（`use_cache=False` 由调用方传）。

---

### P2-4 枚举失败被当成「没有进程」，并写入负缓存

**位置**：`live_token.py:150-154` 与 `:339-342`

```python
table = common.list_processes()          # 失败返回 None（≠ 没进程）
if not table:
    return []                            # ← 两种语义被合并
...
pids = client_pids(ch)
if not pids:
    _remember(key, {}, None, now, use_cache)   # ← 失败也写空缓存，60s 内都判「无凭据」
    return None
```

**影响**：与 `client_pids` 自己的 docstring（「检测不出来返回 None ≠ 没有进程」）自相矛盾。
偶发枚举失败会让 **60 秒内所有请求**误报「桌面端当前未登录」。

**改进**：`client_pids` 区分三态（`None` = 枚举失败 / `[]` = 真没有 / 非空）；
`read_live_session` 只在「真没有进程」时写负缓存，枚举失败**不写缓存**直接返回 `None`。

---

### P2-5 `_recalc_expiry` 裸 `tmp.replace`，续期成功却报 500

**位置**：`wb_ui_server.py:1222-1224`

```python
tmp = Path(path).with_suffix(".info.tmp")
tmp.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
tmp.replace(path)                    # 未用 common.replace_with_retry，也无 try/except
```

**影响**：续期已在 `refresh_account_file:1261` 判定为 `refreshed` 且 `ok`，
随后这一步若被杀软/客户端占用抛 `OSError`，异常冒到 HTTP 层变成 **500** ——
**token 其实已续，用户看到失败**；还可能残留 `.info.tmp`。
同文件其它写盘点都用了 `replace_with_retry`，这里是漏网。

**改进**：改用 `common.replace_with_retry(tmp, path)`，整段包 `try/except OSError`，
失败仅降级为「有效期显示未刷新」的提示。

---

### P2-6 积分 / 签到缓存无 single-flight，回源在锁外

**位置**：`wb_ui_server.py:1673-1680`（credits），`:1383-1389`（checkin 同型）

```python
with _CREDITS_LOCK:
    _c = _CREDITS_CACHE.get(ch.key) or {}
    cached, ts = _c.get("payload"), _c.get("ts", 0.0)
    if not force and cached and now - ts < CREDITS_TTL:
        return dict(cached, cached=True)
# ← 锁已释放，下面才开始 N 次网络请求
cfg = channel_cfg(ch)
accounts = list_accounts(ch)
```

**影响**：锁只保护读/写缓存，不保护回源。启动预热线程与首个请求、
或多个 `?force=1` 并发时会**各自跑完整扇出**（每账号一次外部请求）。
按每账号 ~700ms 计，N 个账号并发 2 次就是 2N 次外部调用，页面卡顿 + 网关限流风险。

**改进**：加 per-channel 「进行中」`threading.Event` 做单飞 ——
未命中但已有 fetch 在跑则等待其结果，而不是再扇出一次。

---

### P2-7 热路径重复读盘 / 重复解析（N+1）

**位置**：`wb_ui_server.py:352-353`、`:1398`、`:1690`

```python
acc = wb.session_from_info_file(f)     # 读 + 解析
raw = _read_info(f) or {}              # 同一个文件再读 + 再解析
...
acc = wb.session_from_info_file(ch.auth_dir / a["file"])   # snapshot 里第三次
```

**影响**：`list_accounts` 每文件读两遍；`credits_snapshot` / `checkin_snapshot` 的 `_one`
再解析一遍；`uid_nickname_map` 又把两个目录的全部文件读一遍。
N 个账号 ≈ 3N+ 次磁盘读 + JSON 解析，被 `/api/accounts` 与每次积分/签到刷新反复触发。

**改进**：`list_accounts` 在内部保留解析结果（`acc`/`raw`）供 snapshot 复用；
`uid_nickname_map` 结果按通道加短 TTL 缓存。

---

### P2-8 `read_state` 返回 `"CORRUPT"` 字符串，`or {}` 兜不住

**位置**：`window_state_guard.py:476`

```python
st = read_state(ch) or {}          # read_state 损坏时返回字符串 "CORRUPT"
entry = _golden_entry(b, maximized, st.get("isFullScreen", False))   # AttributeError
```

同文件 `:447`、`:544`、`:618` 都写对了（`if st in (None, "CORRUPT")`），只有这里漏。

**影响**：状态文件损坏时 `capture` 命令直接崩，而不是降级。

**改进**：`st = read_state(ch); st = {} if st in (None, "CORRUPT") else st`
（与其余三处保持一致，或抽一个 `read_state_or_empty(ch)`）。

---

### P2-9 `ui_hub` 侧边栏 `reason` 未转义进 `innerHTML`

**位置**：`ui_hub.html:259`

```js
v.cur.innerHTML = '<span class="dot"></span>' + ((c && c.reason) || '桌面端当前未登录');
```

同一个函数下一行（`:263`）却用了 `esc()`。`reason` 可能内嵌文件名
（如 `tw_ui_server.py:298` 的 `"%s 不是合法 JSON" % sp.name`）。

**影响**：全局侧边栏的注入面 + 同函数写法自相矛盾。当前数据源都是本机文件名，
实际可利用性低，但属「不该留的口子」。

**改进**：`esc(c.reason)`。

---

### P2-10 首屏三请求并行 + `renderAll` 无就绪守卫 → 闪假空态

**位置**：`ui_template.html:1053-1055` + `:773`

```js
// 三件事互不依赖：一起发，谁先回来谁先渲染（renderAll 可重复调用）。
load();
loadCredits();
loadCheckin();
```

```js
function renderAll(){ renderCurrent(); renderAccounts(); }   // 不检查数据是否就绪
```

`renderCurrent()`（`:778-781`）在 `lastCurrent` 为空时渲染
**「无法获取桌面端当前账号。」**；`renderAccounts` 在 `lastList` 为空时渲染
**「wb_auth\ 目录下没有账号文件。」**。

**影响**：服务端启动会预热积分缓存，credits 常先返回 → `renderAll()` 在
`lastCurrent=null、lastList=[]` 下执行 → **每次打开页面先闪一段「账号库为空 / 读不到当前账号」的假故障**。
并行化本身是对的（注释已说明收益），缺的是「未就绪不渲染」这一道守卫。

**改进**：`function renderAll(){ if(!lastCurrent && !lastList.length) return; ... }`，
或首屏用 `Promise.all` 后再首次渲染、之后才允许各自刷新。

---

### P2-11 敏感备份堆积且无保留策略

**位置**：`account_migration.py:804-824`、`window_state_guard.py:272-285`

- `.migration-backup/<ts>/workbuddy.db` 是**完整对话库快照**，保留 3 份，存在用户数据目录内；
- `window_state_backups/` 只增不删。

**影响**：长期驻留的敏感数据（全部对话内容），且用户不知道它们存在。

**改进**：迁移备份加期限/数量双策略并写进 README；
`window_state_backups/` 复用 `common.prune_backups` 设保留上限。

---

### P2-12 `import_token.py` 凭据非原子落盘

**位置**：`import_token.py:309-317`

```python
out.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
```

**影响**：直接覆写含 `refresh_token` 的 `config.json`，中断即截断 → **全部账号凭据丢失**。
`chmod 0600` 只在 POSIX 生效，Windows 下无权限收紧。

**改进**：临时文件 + `os.replace` 原子替换；Windows 侧用 `icacls` 收权。

---

### P2-13 cmd / ps1 脚本编码与退出码

**位置**：`refresh_all.cmd`、`trae_refresh_all.cmd`、`workbuddy_switcher.cmd`、
`trae_switcher.cmd`、`install_refresh_task.ps1`

**实测**：四个 `.cmd` 均为 **UTF-8 无 BOM + CRLF 行尾**（`od -c` 看第 1 行是 `@echo off\r\n`），
未 `chcp 65001` → GBK 控制台下中文提示全乱码；
`install_refresh_task.ps1` 同样无 BOM，Windows PowerShell 5.1 按 ANSI 解码 → `Write-Host` 乱码。

> 勘误（09-24 01:30 复核）：本条初版把行尾写成「LF-only」，实测是 **CRLF**，已改正。
> 结论不受影响 —— 乱码的根因是**无 BOM 的 UTF-8 内容**被按 GBK/ANSI 解码，与行尾无关。

另：`workbuddy_switcher.cmd:33-39` / `trae_switcher.cmd:31-37` 只判 `%ERRORLEVEL%==1`，
**末尾无 `exit /b`，脚本恒返回 0**（`refresh_all.cmd:39` 是对的，三份不一致）。

**改进**：cmd 存为 ANSI 或加 `chcp 65001 >nul`；ps1 存为 **UTF-8 with BOM**；
统一 `if errorlevel 1` 并在末尾 `exit /b %RC%`。

---

## 五、P3 —— 可维护性 / 一致性

| # | 位置 | 问题 | 改进 |
|---|---|---|---|
| P3-1 | `switcher_common.py:1253` | 错误审计用 `self.SOURCE`，而 `:1314/1319` 用 `audit_source(path)` → `/api/wbai/*` 出错记成 `[wb]`（`wb_ui_server.py:1812` 有覆盖） | 改为 `self.audit_source(path)` |
| P3-2 | `switcher_common.py:1299` | `do_GET` 调 `_handle_request_error(e)` 不传 `path` → 函数内 `if self.AUDIT_DIR and path` 恒假 → **GET 异常零审计** | 传 `u.path` |
| P3-3 | `switcher_common.py:916` / `:966` | `probe_instance_range` 与 `_probe_all` 逻辑几乎重复（线程池 + 最小端口 + 串行兜底） | `_probe_all` 复用前者，增 `exclude_pid` |
| P3-4 | `switcher_common.py:1096` | `open_page` 先 `mark_page_opened()` 再 `webbrowser.open`，失败仍占 30s 宽限期 | 成功后再 mark |
| P3-5 | `switcher_common.py:634` | tasklist 兜底裸 `split(",")` 解析 CSV，进程名含逗号整行丢弃 | 改用 `csv.reader` |
| P3-6 | `switcher_common.py:80` | `replace_with_retry` 在 `tries<=0` 时 `raise last`（`None`）→ `TypeError` | `tries = max(1, int(tries))` |
| P3-7 | `wb_ui_server.py:744` | `winreg.OpenKey` 的 `root` / `sk` 句柄从不 `CloseKey` → 长跑句柄累积，且每次 TTL 过期重走注册表 | `with` 或显式关闭 + 结果按进程生命周期缓存 |
| P3-8 | `wb_ui_server.py:1178` | `_NullLog` 模块级单例只 append 不清空 | 直接丢弃或改环形缓冲 |
| P3-9 | `wb_ui_server.py:1754` | `build_ui_context` 硬编码标题，与 `Channel.title` 重复维护；`CHANNELS` 无 Trae key 但留了分支 | 用 `ch.title`，删残留分支 |
| P3-10 | `tw_ui_server.py:325` | `tasklist` 用 `encoding="utf-8"` 解码而注释自述输出是 GBK；且是**第二份进程枚举实现** | 复用 `common.list_processes()` |
| P3-11 | `tw_ui_server.py:530` | `load_config` 在 `try` 之外，两处 config 都不存在时抛异常 → `/api/refresh` 500、`_refresh_all_collect` 整批中断 | 包进 try + 默认配置兜底 |
| P3-12 | `tw_ui_server.py:394` | `sp.read_bytes()` 在 `try` 之外 → 文件被占用时 500 而非可读错误 | 纳入 try |
| P3-13 | `tw_ui_server.py:378/421` | `target_uid` 解析失败时为空 → 写后自校验**必然假阴性**，报「未采纳」误导用户重试 | 为空时降级比对密文或跳过自校验 |
| P3-14 | `tw_ui_app.py:48-127` / `wb_ui_app.py:56-143` | `_port_alive`/`_parse_args`/`_show_window`/`_log_startup` 逐字重复；`_port_alive` **全程未被调用**（死代码） | 下沉 `switcher_common`，删死代码 |
| P3-15 | `check_exe_datadir.py:106` | `taskkill /IM WorkBuddySwitcher.exe /F` **按镜像名全杀**，会杀掉用户正在运行的实例（自检工具的破坏性副作用） | 只结束本次 `Popen` 的 pid 树 |
| P3-16 | `WorkBuddySwitcher.spec:33` | `datas` 未含 `window_state_guard.py` —— 而它正是「窗口被自动还原」的修复产物，只有 exe 的用户用不到 | 一并随包分发 |
| P3-17 | `ui_template.html:942` / `:568` | toast 双重转义（`toast` 用 `textContent`，调用处又 `esc()`）；`ttlTag` 无天数时兜底写死 `'55天'`（国际服实测约 362 天） | 去掉多余 `esc`；兜底改「长期」或不显示 |
| P3-18 | `ui_template.html` / `ui_hub.html` | `esc`、`closeThisTab`、心跳**逐字重复**（约 40 行）。⚠️ 初版把「频道名不一致」也列为缺陷，**核验后是误报** —— 见第十七章 | 见第十七章建议 |
| P3-19 | `ui_hub.html:146/192` | 硬编码 `http://127.0.0.1:8765/`，而端口会自动顺延（`wb_ui_server.py:1979`）→ 顺延后离线提示指向错地址 | `build_ui_context` 增 `{{BASE_URL}}` |
| P3-20 | `ui_template.html:773` | `renderAll` 全量重建 `#curList`+`#acctList`，叠加最多 6 次 `settleAfterSwitch` 轮询 → 首屏最多约 8 次全量重排 | rAF 合并渲染或按行 diff |
| P3-21 | `ui_template.html:650` | `loadCredits` 失败只改按钮文案、不清 `creditsMap` → 旧积分继续当最新值显示 | 失败时标记陈旧 |

---

## 六、逐模块审查结论

| 模块 | 结论 | 关键依据 |
|---|---|---|
| **编译与回归自检** | ✅ 健康 | 508 PASS / 0 FAIL；`live_token` 性能断言与行为级桩都在 |
| **HTTP 骨架** | ⚠️ 可用，有加固空间 | 路由/守卫/令牌/路径校验主体正确（14 个写端点 GET 全 405、路径穿越 5 种全拒）；问题集中在 P2-1/2-2 与 P3-1/3-2 |
| **单实例保护** | ✅ 正确 | `probe_instance` 能认出自己、端口顺延正常；仅 P3-3 重复实现 |
| **文件安全** | ✅ 正确 | `file_lock` 超时精确、`safe_unlink`/`safe_rmtree` 含 `BaseException` 兜底；P3-6 是边界语义 |
| **审计日志** | ⚠️ 有缺陷 | 内容脱敏正确，但 **P3-1 来源标记错** + **P3-2 GET 异常零审计** |
| **账号切换** | ❌ **有真实缺陷** | **P1-3 客户端不恢复**、**P1-4 锁不互斥**；P2-5 续期写盘 |
| **凭据与降级** | ✅ 正确 | 字段保护降级链路完整；`LIVE_REASON` 确实进不了渲染路径 |
| **内存凭据 `live_token`** | ⚠️ 有缺陷 | 性能已优（冷 0.122s）；**P2-3 缓存忽略 want_refresh**、**P2-4 枚举失败合并语义** |
| **积分 / 签到** | ⚠️ 有缺陷 | 缓存分区正确；P2-6 无 single-flight、P2-7 重复解析 |
| **数据迁移** | ❌ **最严重** | **P0-1 必抛 AttributeError**、**P1-1 回滚不还原目录**、**P1-2 manifest 只在最后落盘** |
| **Trae 侧** | ⚠️ 有缺陷 | **P1-5 设备键只增不删**；P3-10~13；`refresh_all` 无门卫属**用户已决定暂不处理** |
| **窗口守卫** | ⚠️ 有缺陷 | P2-8 `"CORRUPT"` 处理漏一处；备份无保留策略 |
| **前端两份模板** | ⚠️ 有缺陷 | **P1-6 判据漂移**；P2-9/2-10；占位符三通道**无漏配**（核验通过） |
| **启动器** | ✅ 可用 | `rebind()` 唯一入口、CLI 转发成对；P3-14 重复 + 死代码 |
| **辅助脚本 / 打包** | ⚠️ 有缺陷 | P2-12/2-13、P3-15/3-16 |

---

## 七、建议的实施优先级

| 优先级 | 项目 | 理由 | 预计改动量 |
|---|---|---|---|
| **1** | **P0-1** 改 `safe_unlink_tree` → `safe_rmtree` | 一行；不修则每次「切回已存在的账号」都触发 | 1 行 |
| **2** | **P1-3** 切号 `try/finally` 恢复客户端 | 用户可见（客户端莫名被关）+ 报告与状态相反 | ~10 行 |
| **3** | **P1-6** `currentMatches` 补 `c.live` | 一行；不修则字段保护下切号反馈完全失效 | 1 行 |
| **4** | **P1-4** 统一账号锁 key | 数据面竞态；需谨慎设计避免死锁 | ~15 行 |
| **5** | **P1-1 + P1-2** 回滚消费目录快照 + 增量 manifest | 与 P0-1 同一条路径，建议一起修 | ~30 行 |
| **6** | **P2-3 + P2-4** `live_token` 缓存键与三态语义 | 诊断结论可信度 | ~15 行 |
| **7** | **P2-1** `_params` 加长度上限 | 一行级加固，防挂线程 | ~5 行 |
| **8** | **P2-2** 页面路由移到 `_guard()` 之后 | 纵深防御 + 与 README 一致 | ~5 行 |
| 9 | P1-5 Trae 设备键、P2-5~2-13、P3 全部 | 按需 | — |

**每修一项都要补 smoke 断言**（这个项目的既有做法：P0-1/P1-3/P1-4/P1-6 都属「能静默复发」的类型）。

---

## 八、核验后撤销 / 明确不算缺陷的项

| 代理提出 | 核验结论 |
|---|---|
| 页面路由绕过守卫「令牌泄漏可被利用」（报 P1） | **降为 P2**。`do_POST` 的 `_guard()`（Host + Origin）与令牌校验**串联**，DNS rebinding 拿到令牌也过不了 Host 校验。属纵深防御不一致，非可利用漏洞 |
| 前端占位符可能漏配 | **非缺陷**。14 个 `{{KEY}}` 在 wb / wbai / tw 三处**全部配齐** |
| `client_status` 无缓存（历史报告 P2） | **误报**。已有 4s TTL + `fresh=True`；本次实测冷 0.006s |
| 内存扫描建议并行化 | **已证无效**（跨进程 `ReadProcessMemory` 全局竞争，workers=7 比串行慢） |
| 建议「进程内命中即停」 | **不采纳**。命中点 65MB/230MB 只省 ~0.06s，但可能取到偏旧的同 uid token |
| `ok===false` 视为异常 | **非缺陷**。客户端 5.6.2+ 字段保护下是常态，界面靠 `live_token` 补 |
| Trae `refresh_all()` 无门卫 | **确认为真**，但属**用户已明确决定暂不处理**的遗留项，本次仅登记 |
| Trae `20403 Token device not match` | **用户已决定暂不处理**；P1-5 是**另一处**同类问题的根因 |
| `.trash_20260914/` 未清理 | **用户已决定保留** |

---

## 九、审查方法与覆盖度

- **覆盖**：`switcher_common.py`(1381) / `wb_ui_server.py`(2082) / `tw_ui_server.py`(732) /
  `wb_ui_app.py`(226) / `tw_ui_app.py`(204) / `live_token.py`(392) / `account_migration.py`(865) /
  `window_state_guard.py`(760) / `import_token.py`(374) / `check_*.py`(486) /
  `ui_template.html`(1058) / `ui_hub.html`(340) / 两个 `.spec` / 四个 `.cmd` / `install_refresh_task.ps1`
  —— **源码与配置 100% 覆盖**。
- **未逐行覆盖**：`smoke_test.py`(2647) 仅按需查证引用；`README*.md` / `DESIGN_*.md` / `AUDIT_*.md`
  仅用于交叉核对声明与实现是否一致。
- **实证手段**：`hasattr` 验证（P0-1）、源码原文比对（P1-1~P1-4、P2-2、P2-8、P2-10）、
  计时（性能结论）、与 `tasklist` 对拍（进程枚举）、三模式页面渲染验证。
- **已知盲区**：P1-5 对 Trae 客户端的实际影响未做端到端复现（需真实 Trae 登录态）；
  P2-6 的并发扇出未做压力复现（需并发注入）。

---

## 十、修复记录（2026-09-24，按第七章优先级 1~8 逐项完成）

**回归自检**：`smoke_test.py` **530 PASS / 0 FAIL，退出码 0**（修复前 508 → +22 条新钉）

### 逐项

| 优先级 | 项 | 动作 | 实证 |
|---|---|---|---|
| 1 | **P0-1** | 删掉 `common.safe_unlink_tree` 调用；合并分支改为把旧目录 **移进备份目录** 并登记 `journal.renames` | 真跑合并分支：11/11 PASS |
| 2 | **P1-3** | `_restore_client()` 闭包 + `try/finally`；`switched_ok` 区分成功/失败路径 | 桩测：失败后 `open_client` 被调用 1 次 |
| 3 | **P1-6** | `currentMatches` 补 `\|\| c.live` | 源码断言 + 三处判据同源 |
| 4 | **P1-4** | 切号加**两把锁**（通道 → 目标账号），顺序固定不死锁 | 源码断言：`file_lock` 计数 = 2 |
| 5 | **P1-1** | `_merge_storage_dir` 返回新建文件列表 → `journal.merged_files`；`_rollback` 删掉这些副本 | 桩测：回滚后旧目录还原、新目录无残留 |
| 6 | **P1-2** | 新增 `_Journal.flush()`，`add_step()` 每步落盘 manifest | 合并分支后 `manifest.json` 含 `merged_files` |
| 7 | **P2-3** | 缓存记下 `want_refresh`，诊断档不吃功能档那份缓存（反向仍可复用） | 行为桩：先功能档后诊断档仍拿到 refresh |
| 8 | **P2-4** | `client_pids` 三态（`None`/`[]`/列表）；枚举失败**不写负缓存** | 行为桩：枚举失败 → `None` 且 `_CACHE` 为空 |
| 9 | **P2-1** | `MAX_BODY_BYTES` + `BadRequest` → 400 | 原始 HTTP：非数字/负数/超大 → **400** |
| 10 | **P2-2** | 页面路由移到 `_guard()` 之后（`/api/ping`、`/api/page-alive` 仍豁免） | 原始 HTTP：`Host: evil.com` 取 `/`、`/wb` → **403**；回环 → 200 且注入令牌 |

### 改动文件

| 文件 | 改动 |
|---|---|
| `account_migration.py` | P0-1 合并分支改走备份目录；`_merge_storage_dir` 返回新建文件；`_Journal.merged_files` + `flush()`；`_rollback` 清合并残留 |
| `wb_ui_server.py` | P1-3 `_restore_client()` + `try/finally`；P1-4 双锁 |
| `switcher_common.py` | P2-1 `MAX_BODY_BYTES` / `BadRequest` / `_params` 校验 / 400 映射；P2-2 页面路由下移 |
| `live_token.py` | P2-3 缓存键含 `want_refresh`；P2-4 `client_pids` 三态 + 失败不写缓存 |
| `ui_template.html` | P1-6 `currentMatches` 补 `c.live` |
| `smoke_test.py` | +22 条回归钉；两处绑在旧代码形状上的断言改成行为级 |

### 修复过程中踩的坑（同一个坑的第三、四次）

1. **断言又被自己的注释绊倒**：`"safe_unlink_tree" not in src` 因为我在注释里写了
   `` `common.safe_unlink_tree` `` 而假 FAIL → 改成只匹配**调用**（带左括号）。
   （同类：09-23 断言挂在自己删掉的字面量上；09-24 断言 `buf.raw` 撞上自己的 docstring。）
2. **`split("def ")` 抽取函数体被嵌套函数截断**：switch_account 里新增了
   `def _restore_client()`，旧断言 `src.split("def ")[0]` 把函数体截在嵌套 def 处，
   于是 `count(...) == 2` 恒为 1 → 改成按**列 0 的 `\ndef `** 切。
3. **探针自己写错**：验证「页面注入了令牌」时只读了响应前 200 字节，而令牌在
   `<head>` 里、前面有一大段内联 CSS → 读满 6000 字节才对。
   另一处是 `bind_server` 不设 `Handler.TOKEN`（那是 `serve()` 干的），
   探针里得手动打开，否则「令牌已注入」根本无从验证。
4. **重打前差点误删数据目录**：`dist/TraeSwitcher/tw_auth/` 已存在
   （只有占位 `把账号文件放这里.txt`，无真实账号）—— 这一步按 MEMORY 的约定先 `ls` 才没出事。

### 仍未修（第 9 行「按需」项，等确认）

**P1-5**（Trae 设备键只增不删）+ P2-5 ~ P2-13 + 全部 P3。
其中 P1-5 虽属 P1，但修它要动 Trae 登录态的写入逻辑，而「素材缺设备键时该报错还是照切」
是**行为决策**，且我未做端到端复现（需真实 Trae 登录态）—— 建议单独一轮再动。

---

## 十一、exe 重打与包内容验证（2026-09-24 收尾）

**「源码修好了」≠「exe 修好了」。** `switcher_common.py` 是**两个 exe 共用**的，
按 MEMORY 第 6 条约定两个都必须重打 —— 已完成：

```
WB EXIT=0   dist/WorkBuddySwitcher/WorkBuddySwitcher.exe   09-24 01:19
TW EXIT=0   dist/TraeSwitcher/TraeSwitcher.exe              09-24 01:20
```

### 验证方式：解析 PyInstaller 归档（静态验包）

原本打算直接起服务打 HTTP 探针，结果**被单实例守卫挡住**：本机 8765 上还跑着一个旧实例
（源码方式启动的 `python.exe`，pid 11740，`/api/ping` 自报版本 `wb-20260923-234616`），
新起的 exe 会被判成 `reuse` 并**直接退出**。这是设计行为（见 `DESIGN_single_instance.md`），
不该为了测试去杀用户的实例，于是改成静态验证包内容：

`CArchiveReader` → 取 `PYZ.pyz` → 逐模块 `zlib.decompress` + `marshal.loads` → 得到真正的
**code object**，于是可以做两类断言：

- ① **标识符 / 字面量**：函数名、变量名、字符串在 `.pyc` 里都是明文；
- ② **字节码结构**：像 P2-2「页面路由必须排在 `_guard()` 之后」这种**纯顺序**改动，
  注释会被编译器丢掉，只能反汇编看指令先后。

结果：

| 包 | 结果 |
|---|---|
| `WorkBuddySwitcher.exe` | **16/16 PASS** |
| `TraeSwitcher.exe` | **8/8 PASS，8 项 SKIP**（国服专属模块本就不在该包里） |

最硬的一条是 P2-2 的字节码结构：`/api/ping`（第 9 条）→ `_guard`（第 64 条）→ `/`（第 71 条）
—— 豁免端点在守卫之前、页面路由在守卫之后，正是修复意图。
`ui_template.html` / `ui_hub.html` 与源码 **md5 逐字节一致**（`2cd6b81f` / `b2197d57`）。

### Trae 侧同构发现（未修，按既定决定暂不处理）

顺带核了 Trae 侧：

- **P1-3 不存在** —— `tw_ui_server.py` 没有 `close_client` / `open_client`，它不关客户端；
- **P1-4 同构问题存在** —— `switch_account` 拿的是 `tw-storage` 锁，而续期 / 签到拿的是
  `tw-auth-<base>` 锁，两把锁不互斥。切号**读**素材、续期**写**素材，存在「读到写了一半的
  素材」的窗口。严重程度低于国服侧（不是同一文件双向写），且 `_read_json` 解析失败会明确报
  「素材登录态无法解析」而不是写坏数据 → 判为 **P2**。

按 MEMORY 里「Trae 侧遗留问题暂不处理，除非用户主动提起」的既定决定，**本次不动**，仅登记在此。

### 遗留：必须重启工具，修复才生效

8765 上的旧实例（pid 11740）跑的是 `wb-20260923-234616`，**不含本次任何修复**。
需要关掉它、改从新 exe 启动。新 exe 启动后可用 `/api/ping` 的 `version` 字段确认
是否已是 `wb-20260924-011304`。

---

## 十二、未修项总览（2026-09-24 01:30 逐条回源码复核）

共 **31 项**未修：**P1-5 ×1、P2-5 ~ P2-13 ×9、P3-1 ~ P3-21 ×21**。

> **更新（09-24 01:36）**：其中 **A 组 6 项 + B 组 1 项 + D 组 P2-10 + E 组 P2-12，共 9 项已修复**
> （详见第十四章）。下表保留复核时的原始状态 —— **未修项现为 22 项**（C / F / G 组）。

「复核」列 = 本次 grep 回源码确认的**当前**状态，不是照抄第七章。

### A. 一行级、低风险 —— 建议顺手修（6 项）

| 项 | 位置 | 改动 | 复核 |
|---|---|---|---|
| **P2-8** | `window_state_guard.py:476` | `read_state(ch) or {}` → 与同文件 `:543/:617` 一致的 `in (None, "CORRUPT")` | ✅ 仍未修 |
| **P2-9** | `ui_hub.html:259` | `c.reason` → `esc(c.reason)`（同函数 `:263` 已有 `esc`） | ✅ 仍未修 |
| **P2-5** | `wb_ui_server.py:1259` | 裸 `tmp.replace(path)` → `common.replace_with_retry` + `try/except OSError` | ✅ 仍未修 |
| **P3-1** | `switcher_common.py:1284` | `self.SOURCE` → `self.audit_source(path)`（否则 `/api/wbai/*` 出错记成 `[wb]`） | ✅ 仍未修 |
| **P3-2** | `switcher_common.py:1338` | `_handle_request_error(e)` → 传 `u.path`（`do_POST:1365` 已传，GET 侧漏） | ✅ 仍未修 |
| **P3-6** | `switcher_common.py:80` | `tries = max(1, int(tries))`，避免 `tries<=0` 时 `raise None` → `TypeError` | 报告 |

### B. 一行级，但**必须重打 exe**（1 项）

| 项 | 位置 | 改动 | 复核 |
|---|---|---|---|
| **P3-16** | `WorkBuddySwitcher.spec:33` | `datas` 补 `window_state_guard.py` —— 否则 exe 用户拿不到「窗口被自动还原」的修复产物 | ✅ 仍未修（datas 里确实没有） |

### C. 需要设计 / 行为决策（3 项）

| 项 | 位置 | 待决问题 |
|---|---|---|
| **P1-5** | `tw_ui_server.py:363-404` | 素材缺设备键时「明确报错」还是「照切」？且需真实 Trae 登录态做端到端复现 |
| **P2-6** | `wb_ui_server.py:1673` / `:1383` | single-flight 用 per-channel `Event` 还是 `Future`？要覆盖「预热线程 vs 首个请求」的竞态 |
| **P2-7** | `wb_ui_server.py:352` / `:1398` / `:1690` | 改 `list_accounts` 返回契约（把 `acc`/`raw` 一并带出）会影响所有调用方 |

### D. 用户可感知的体验问题（2 项）

| 项 | 位置 | 症状 |
|---|---|---|
| **P2-10** | `ui_template.html:773` | 首屏 `renderAll` 无就绪守卫 → 每次打开页面先闪一段「账号库为空 / 读不到当前账号」的假故障 | ✅ 仍未修 |
| **P3-17** | `ui_template.html:942` / `:568` | toast 双重转义；`ttlTag` 无天数时写死 `'55天'`（国际服实测约 362 天） |

### E. 数据 / 安全面（2 项）

| 项 | 位置 | 风险 |
|---|---|---|
| **P2-12** | `import_token.py:309/311` | 直接 `write_text` 覆写含 `refresh_token` 的 `config.json`，中断即截断 → **全部账号凭据丢失**；`chmod 0600` 在 Windows 下无效 | ✅ 仍未修 |
| **P2-11** | `account_migration.py:804` / `window_state_guard.py:272` | `.migration-backup/` 存**完整对话库快照**且无期限策略；`window_state_backups/` 只增不删 |

### F. 其余（按需）

- **P2-13** 脚本编码与退出码 —— `workbuddy_switcher.cmd:33` / `trae_switcher.cmd:31` 末尾无 `exit /b` → **脚本恒返回 0**（`refresh_all.cmd:39` 是对的）；四个 `.cmd` 无 BOM、无 `chcp`；
- **P3-3 / P3-4 / P3-5 / P3-7 / P3-8 / P3-9** 内部一致性（重复实现、`winreg` 句柄泄漏、`_NullLog` 单例只增不减）；
- **P3-10 ~ P3-13** Trae 侧健壮性（按用户既定决定暂不处理）；
- **P3-14 / P3-18** 改动面大的重构（下沉重复代码、抽 `ui_common.js`）—— 收益一般、回归风险高，建议**不动**；
- **P3-19 / P3-20 / P3-21** 前端细节（硬编码端口、全量重排、失败不清陈旧积分）。

### G. 用户已明确决定不处理（不再提议）

Trae `refresh_all()` 无门卫、Trae `20403 Token device not match`、`.trash_20260914/` 保留。

---

## 十三、复核勘误

| 位置 | 初版写的 | 实测 | 处理 |
|---|---|---|---|
| 本报告 P2-13 | 四个 `.cmd` 为 **LF-only** | **CRLF**（`od -c` 第 1 行 `@echo off\r\n`） | 已改正；乱码根因是无 BOM 的 UTF-8 被按 GBK/ANSI 解码，与行尾无关，结论不变 |

---

## 十四、第二轮修复记录（2026-09-24 01:36，第十二章 A/B/D/E 组 9 项）

**回归自检**：`smoke_test.py` **544 PASS / 0 FAIL，退出码 0**（上一轮 530 → **+14 条新钉**，含 3 条行为级）

### 逐项

| 组 | 项 | 位置 | 改动 |
|---|---|---|---|
| A | **P2-8** | `window_state_guard.py:476` | `read_state(ch) or {}` → `if not isinstance(st, dict): st = {}`。损坏时 `read_state` 返回**字符串** `"CORRUPT"`，非空字符串是真值 → `or {}` 不生效 → 下一行 `.get()` 直接 `AttributeError`，`capture` 崩掉而非降级 |
| A | **P2-9** | `ui_hub.html:259` | `c.reason` → `esc(...)`，与同函数 `:263` 对齐（原先同一函数两种写法自相矛盾） |
| A | **P2-5** | `wb_ui_server.py:1253` | `_recalc_expiry` 改用 `common.replace_with_retry` + `try/except OSError`；**失败返回 `False`**，调用方改成「已续期 %s（有效期显示没能写回，下次刷新会重算）」—— 不再把"已续期"误报成失败 |
| A | **P3-1** | `switcher_common.py:1284` | 错误审计 `self.SOURCE` → `self.audit_source(path)`（`/api/wbai/*` 出错不再记成 `[wb]`） |
| A | **P3-2** | `switcher_common.py:1291` | `do_GET` 补 `path = ""` / `path = u.path`，异常时把 `path` 传给 `_handle_request_error` —— 否则 `if self.AUDIT_DIR and path` 恒假，**GET 侧异常零审计** |
| A | **P3-6** | `switcher_common.py:80` | `range(int(tries))` → `range(max(1, int(tries)))`（`tries<=0` 时原先 `raise None` → `TypeError`，把真正的 `OSError` 吃掉） |
| B | **P3-16** | `WorkBuddySwitcher.spec:33` | `datas` 补 `('window_state_guard.py', '.')` —— 这是「窗口被自动还原」的修复产物，原先只有 exe 的用户拿不到 |
| D | **P2-10** | `ui_template.html:773` | `renderAll` 加就绪守卫 `if(!lastCurrent && !(lastList && lastList.length)) return;` —— 首屏三请求并行、credits 常先返回，导致先渲染「账号库为空」假故障 |
| E | **P2-12** | `import_token.py:298` | 新增 `_atomic_write_text()`（**同目录**临时文件 + `fsync` + `os.replace`），`merge_write` 两处改用它 —— 原先直接 `write_text` 覆写含 `refresh_token` 的 `config.json`，中断即截断 → **全部账号凭据丢失** |

### 改动文件

| 文件 | 改动 |
|---|---|
| `window_state_guard.py` | P2-8 dict 兜底 |
| `ui_hub.html` | P2-9 补 `esc()` |
| `wb_ui_server.py` | P2-5 原子替换 + 降级返回 `bool` + 调用方如实提示 |
| `switcher_common.py` | P3-1 `audit_source`；P3-2 `do_GET` 传 `path`；P3-6 `tries` 下限 |
| `WorkBuddySwitcher.spec` | P3-16 `datas` 补 `window_state_guard.py` |
| `ui_template.html` | P2-10 就绪守卫 |
| `import_token.py` | P2-12 原子落盘（`import tempfile` + `_atomic_write_text`） |
| `smoke_test.py` | +14 条新钉（含 3 条行为级） |

### 踩的坑（**同一个坑的第五、六次**）

1. **`_code21` 只剥 `#` 注释、漏了 docstring**：负向断言 `"out.write_text(" not in src`
   撞上我自己写在 `_atomic_write_text` **函数 docstring** 里的旧写法说明 → 假 FAIL。
   **修法**：用 `ast` 遍历移除**所有** docstring（模块 / 类 / 函数，含嵌套）再去 `#` 注释。
   这是本项目**第五次**栽在「断言挂在自己的注释 / 文档上」。
2. **探针不认 Python 3.13 的超指令**：`self._handle_request_error(e, path)` 被编译成
   `LOAD_ATTR` + **`LOAD_FAST_LOAD_FAST ('e', 'path')`** + `CALL 2` ——
   编译器把连续两个 `LOAD_FAST` **合并**了，`argval` 是**元组**。
   只匹配 `opname == "LOAD_FAST" and argval == "path"` 永远找不到 → 假 FAIL。
   另外实参是在 `LOAD_ATTR` **之后**才压栈的，要**往后**看而不是往前看。
3. **验包要认准「用户实际跑的是哪一份」**：`window_state_guard.py` 只是 `datas` 分发的
   独立脚本，**不在 PYZ 里** —— 该验 `_internal/window_state_guard.py` 那份源码，
   而不是去 PYZ 里找模块（否则假 FAIL）。

### exe 重打与验包

```
WB EXIT=0  dist/WorkBuddySwitcher/WorkBuddySwitcher.exe  09-24 01:36
TW EXIT=0  dist/TraeSwitcher/TraeSwitcher.exe            09-24 01:36
```

| 包 | 静态验包结果 |
|---|---|
| `WorkBuddySwitcher.exe` | **11/11 PASS** |
| `TraeSwitcher.exe` | **6/6 PASS**（P3-16 与 `wb_ui_server` 两项 SKIP —— 国服专属，不在该包） |

最直接的证据是 P3-16：`_internal/window_state_guard.py` 存在且与源码 **md5 逐字节一致**（`78f8150f`）；
P3-2 则靠反汇编确认 `do_GET` 调 `_handle_request_error` 时确实压了 `path` 实参。

### 本轮之后仍未修

第十二章的 **C / F / G 组**，共 **22 项**：P1-5、P2-6、P2-7、P2-11、P2-13、P3-3~P3-5、P3-7~P3-14、
P3-17~P3-21，外加用户已明确决定不处理的三项。

---

## 十五、第三轮修复记录（2026-09-24 01:51，第十二章 C/D/F 组 18 项）

**用户决策（本轮动手前确认）**：

- **跳过 P3-14 / P3-18 两处大重构** —— 改动面大、回归风险高、收益一般；
- **P1-5 采取「清掉本机旧设备键后照切」**，而不是"素材缺设备键就报错"；
- **Trae 侧只修 P3-10~P3-13**，`refresh_all` 无门卫继续不动。

**回归自检**：`smoke_test.py` **567 PASS / 0 FAIL，退出码 0**（上一轮 544 → **+23 条新钉**）

### 逐项

| 组 | 项 | 位置 | 改动 |
|---|---|---|---|
| C | **P1-5** | `tw_ui_server.py:401` | 写入前**先删** `cur` 里所有 `iCubeAuthInfo://icube-dc*` 键再写目标键 —— 原先素材里没有的键不会被覆盖，上一任账号的设备凭据原样残留 → `device not match` |
| C | **P2-6** | `wb_ui_server.py` | credits / checkin 回源加**每通道一把锁**（double-checked）：拿到锁后重查缓存，`ts >= t0` 说明是本次等待期间别人刚填的，即便 `force=True` 也复用 —— 否则「预热线程 vs 首个请求」「多个 `?force=1`」各自跑完整扇出 |
| C | **P2-7** | `wb_ui_server.py` | `list_accounts` / `uid_nickname_map` 按**目录签名**（所有 `.info` 的 `mtime_ns` + 大小）在进程内复用；签名一变立即失效 |
| F | **P2-13** | 4 个 `.cmd` + 1 个 `.ps1` | `workbuddy_switcher.cmd` / `trae_switcher.cmd` 末尾补 `exit /b %RC%`（原先恒返回 0）；两个含中文的 `.cmd` 加 `chcp 65001 >nul`；`install_refresh_task.ps1` 补 **UTF-8 BOM** |
| F | **P3-3** | `switcher_common.py:930` | `probe_instance_range` 增 `extra_ports` / `exclude_pid`，成为**唯一**一份端口区间探测；`_probe_all` 改为只传参（原先两边各写一遍线程池 + 串行兜底 + 最小端口） |
| F | **P3-4** | `switcher_common.py:1100` | `open_page` 把 `mark_page_opened()` 挪到 `webbrowser.open` **之后** —— 原先 open 失败也照样占 30s 宽限期，期间调用被误判成「已有页面在运行」 |
| F | **P3-5** | `switcher_common.py:646` | tasklist 兜底改 `csv.reader`（进程名含逗号不再整行丢弃），并 `import csv` |
| F | **P3-7** | `wb_ui_server.py:730` | 注册表两处 `OpenKey` 全部配 `CloseKey`（`try/finally`）—— `find_client_exe` 被反复调用，句柄原先只开不关 |
| F | **P3-8** | `wb_ui_server.py:1208` | `_NullLog` 改**只丢弃**，不再 append 到永不清空的 `_buf` |
| F | **P3-9** | `wb_ui_server.py:1791` | `build_ui_context` 用 `ch.title`，不再硬编码一份标题；LOGO 改 `dict.get` 映射（消掉"else = 国际服"那个坑） |
| F | **P3-10** | `tw_ui_server.py:315` | `_trae_running` 复用 `common.list_processes()`（原生枚举给真 Unicode 名），不再自己跑 tasklist 且用错编码 |
| F | **P3-11** | `tw_ui_server.py:529` | `load_config` 包进 try，两份 config 都不在时返回明确错误而非 500 / 整批中断 |
| F | **P3-12** | `tw_ui_server.py:394` | `sp.read_bytes()` 包进 try（被 Trae 占用时给可读错误） |
| F | **P3-13** | `tw_ui_server.py:421` | `target_uid` 为空时**降级比对密文** —— 原先拿空值比对必然假阴性，误报「未采纳」 |
| D | **P3-17** | `ui_template.html:568/951` | `ttlTag` 兜底不再写死 `'55天'`（拿不到就说「长期」）；toast 调用处去掉多余 `esc()`（`toast` 内部用 `textContent`） |
| F | **P3-19** | `ui_hub.html:146/192` | 离线提示的地址改用 `location.origin` 填 —— 端口顺延后写死的 `8765` 是错的 |
| F | **P3-20** | `ui_template.html:773` | `renderAll` 用 `requestAnimationFrame` 合并同一帧内的多次调用（原先首屏最多 8 次全量重排） |
| F | **P3-21** | `ui_template.html:639` | `loadCredits` 失败时置 `creditsStale`，`creditsBlock` 显式提示「以下为上次查询结果」—— 原先旧积分继续当最新值显示 |

### exe 重打与验包

```
WB EXIT=0  dist/WorkBuddySwitcher/WorkBuddySwitcher.exe  09-24 01:51
TW EXIT=0  dist/TraeSwitcher/TraeSwitcher.exe            09-24 01:51
```

| 包 | 静态验包结果 |
|---|---|
| `WorkBuddySwitcher.exe` | **15/15 PASS** |
| `TraeSwitcher.exe` | **12/12 PASS** |

### 踩的坑（**第六次**栽在同一件事上）

**探针的负向断言被 docstring 绊倒**：`"_buf" not in identifiers(wb_ui_server)` 假 FAIL ——
`identifiers()` 会把 `co_consts` 里的字符串一并收集，而 **docstring 就是 co_consts 里的字符串常量**，
`_NullLog` 的 docstring 里正好写着「原先把每条都 append 到 `self._buf`」。
→ 新增 `names_only()`：只收 `co_names` / `co_varnames` / `co_name`，**不含** `co_consts`。

> 前五次分别是：注释里写了函数名（P0-1 断言）、`split("def ")` 被嵌套 def 截断、
> 断言挂在自己删掉的字面量上、`buf.raw` 撞上自己的 docstring、`_code21` 只剥 `#` 漏了 docstring。
> **规律很清楚：凡是"某写法不该出现"的负向断言，都必须先把注释 + docstring 剥干净。**

### 本轮之后仍未修（4 项）

| 项 | 原因 |
|---|---|
| **P3-14**（两个启动器逐字重复 + `_port_alive` 死代码） | 用户决定跳过（大重构、收益一般、回归风险高） |
| **P3-18**（抽 `ui_common.js`，统一两份前端重复实现） | 同上 |
| Trae `refresh_all()` 无门卫 | 用户明确决定不处理 |
| Trae `20403 Token device not match` | 用户明确决定不处理 |

（`.trash_20260914/` 是用户决定**保留**，不是缺陷。）

---

## 十六、补漏修复（2026-09-24 09:03，P2-11 / P3-15）

**起因**：用户在 09:01 问「修复进度如何」，复核时发现**第十五章的「仍未修」清单漏了两项** ——
第十二章把 P2-11 归在 E 组、P3-15 归在 F 组末尾，但第十五章收尾时只列了
P3-14 / P3-18 / Trae 两项，**把 P2-11 和 P3-15 漏掉了**。
这两项都**不是**"用户决定跳过"，属实实在在的遗漏，当场补修。

**回归自检**：`smoke_test.py` **571 PASS / 0 FAIL，退出码 0**（上一轮 567 → **+4 条新钉**，含 1 条行为级）

| 项 | 位置 | 改动 |
|---|---|---|
| **P2-11** | `window_state_guard.py:272` | 新增 `_prune_backups(keep=BACKUP_KEEP)`（默认 20 份），`backup_state()` 写完即裁剪。原先 `window_state_backups/` **只增不删** |
| **P3-15** | `check_exe_datadir.py:106` | `taskkill /IM WorkBuddySwitcher.exe /F` → `taskkill /PID <proc.pid> /T /F`。原先**按镜像名全杀**，会把用户正在用的实例一起干掉 |

两个实现细节：

- **P2-11 不能复用 `common.prune_backups`**：`window_state_guard.py` 是**独立分发**的
  （随 exe 放在 `_internal/` 下，用户直接 `python` 跑），而 `switcher_common` 的代码在
  exe 的 PYZ 归档里 —— 独立脚本 import 不到。所以自己写了一份最小实现，语义保持一致。
- **P3-15 要带 `/T`（连进程树）**：PyInstaller 引导器会另起子进程，只杀 `proc.pid` 不够。
  注意这与「关用户的实例」那条约定**方向相反** —— 关用户实例用 `/PID` **不带 `/T`**
  （避免误伤），关自己起的测试实例要**带 `/T`**（否则杀不干净）。

### exe 重打与验证

```
WB EXIT=0  dist/WorkBuddySwitcher/WorkBuddySwitcher.exe  09-24 09:03
TW EXIT=0  dist/TraeSwitcher/TraeSwitcher.exe            09-24 09:03
```

这两个文件都是 `datas` 打包的，直接比 md5：

| 文件 | 源码 md5 | 包内 md5 |
|---|---|---|
| `window_state_guard.py` | `18dde27d` | `18dde27d` ✅ |
| `check_exe_datadir.py` | `30fb8fb9` | `30fb8fb9` ✅ |

包内抽查：`_prune_backups` 出现 2 次（定义 + 调用）；`"/PID"` 出现 1 次、`"/IM"` **0 次**。

### 仍未修（4 项，全部是用户明确决定不动）

| 项 | 原因 |
|---|---|
| **P3-14**（两个启动器逐字重复 + `_port_alive` 死代码） | 用户决定跳过（大重构、收益一般、回归风险高） |
| **P3-18**（抽 `ui_common.js`，统一两份前端重复实现） | 同上 |
| Trae `refresh_all()` 无门卫 | 用户明确决定不处理 |
| Trae `20403 Token device not match` | 用户明确决定不处理 |

---

## 十七、P3-14 / P3-18 的处置建议（2026-09-24 09:07，含一处勘误）

用户问这两项的建议。回源码核实时**发现 P3-18 有一条判断是误报**，一并更正。

### 勘误：P3-18 的「频道名不一致」不是缺陷

| 频道名 | 谁在用 | 用途 |
|---|---|---|
| `wb_switcher_hub` | `ui_hub.html`（入口页 `/`） | 认出「入口页被开了第二个」 |
| `wb_switcher_page_wb` | `ui_template.html` **独立打开** `/wb` 时 | 认出「国服视图被开了第二个」 |
| `wb_switcher_page_wbai` | 同上，`/wbai` | 认出「国际服视图被开了第二个」 |

`ui_template.html:414-419` 的注释写得很清楚：

```js
// 嵌入模式（统一入口页的 iframe）不参与重复页面探测：入口页本身负责这件事，
// 视图层再判一次只会把"同一个入口页里的另一个视图"当成重复页面。
if(window.self!==window.top) return;
...
// 频道名带视图标识：国服 /wb 与国际服 /wbai 是两个独立视图，同时开着不算重复
ch = new BroadcastChannel('wb_switcher_page_'+(UI.view||'wb'));
```

README 第 504 行也写着「重复页面探测由**入口页**负责（频道 `wb_switcher_hub`）；
视图层在嵌入模式下不参与」。**三个频道各管各的，是设计而非漂移。** 初版把它算作缺陷是核验疏漏。

### P3-14 建议：只删死代码，不下沉

**建议做**：删掉 `wb_ui_app.py:56` / `tw_ui_app.py:48` 的 `_port_alive`。
全文 grep 只有这两处**定义**、**零调用** —— 纯删除，零风险。

**不建议做**：把 `_parse_args` / `_show_window` / `_log_startup` 下沉到 `switcher_common`。理由：

1. **两个启动器有"有意的差异"**：窗口标题、默认端口、互斥体名、CLI 动作表、`BASE_DIR` 来源都不同。
   下沉后要靠参数区分，读起来反而比两份直白的小文件更难。
2. **启动器是 exe 的唯一起点，且 windowed 构建没有控制台** —— 出问题既看不到报错也拿不到输出，
   排查成本远高于收益。这个项目已经为此吃过亏（`_wants_server_cli` 漏一个动词，
   就变成"跑 CLI 反而弹出窗口"）。
3. **收益有限**：三个函数合计约 80 行，且**没有实际漂移**（不存在"改了一处忘了另一处"造成的 bug）。
4. `_log_startup` 依赖各自的 `BASE_DIR`（冻结态路径），下沉要连 `rebind()` 的语义一起搬，牵一发动全身。

**替代方案（推荐）**：不重构，改用**自检钉住**「该一致的部分必须逐字一致」——
成本几乎为零，却能防住将来真正的漂移。

### P3-18 建议：不抽 `ui_common.js`，改用自检钉住

**真重复**（约 40 行）：`esc()`、`closeThisTab()`、心跳 `beat()`。

**不建议抽 `ui_common.js`**，因为要同时改：`BaseHandler` 新增静态文件路由 +
两个 `.spec` 的 `datas` + 两份 html 的引用方式 + smoke 断言 ——
引入的复杂度和回归面**大于**省下的 40 行。而且心跳那两段**上下文本来就不同**
（入口页写死 `/api/page-alive`，视图层用 `API.pageAlive` 常量）。

**推荐**：把"必须一致"的部分抽成自检断言（逐字比对两份文件里的 `esc` / `closeThisTab` 实现），
一旦漂移立刻报错。`esc` 尤其值得钉 —— **P2-9 就是"同一函数里两处转义写法不一致"**，
转义规则漂移是真实的注入面。

---

## 十八、P3-14 / P3-18 处置结果（2026-09-24 09:11，按第十七章建议执行）

**回归自检**：`smoke_test.py` **587 PASS / 0 FAIL，退出码 0**（上一轮 571 → **+16 条新钉**）

### P3-14：删死代码，不重构

删掉 `wb_ui_app.py` / `tw_ui_app.py` 里的 `_port_alive`（两处定义逐字相同、**全文零调用**）。

**没有**做下沉重构（理由见第十七章），改为在自检里钉住"该保留的必须还在"：

| 断言 | 作用 |
|---|---|
| `_port_alive` 已删 | 防止将来有人又把它抄回来 |
| `_wants_server_cli` 仍在 | 防"删过头" —— 这个函数是 exe **不弹窗**的唯一保障 |
| `_SERVER_ACTIONS` 两个启动器都在 | CLI 转发表必须**成对存在**（MEMORY 第 10 条） |

删完顺手确认 `time.` 仍各被引用 3 次 —— 没有把 `import time` 变成未用导入。

### P3-18：不抽 `ui_common.js`，改用防漂移断言

新增 16 条断言，钉住两份前端里**语义必须一致**的部分，且**只钉安全语义、不钉空格与 `var`/`const`**：

| 对象 | 钉住的 | 为什么 |
|---|---|---|
| `esc()` | 转义正则 + 5 条映射（`& < > " '`） | **这是注入面**；P2-9 正是"同一函数里两处转义写法不一致" |
| `closeThisTab()` | `window.close()` / `dupBar` / `.dup-txt span` / 那句失败文案 | 用户可见文案两样会被当成 bug 报 |
| 心跳 | 间隔 5000 + `visibilitychange` 补拍 | URL **有意不同**（入口页写死、视图层用常量），不钉 |
| 三个频道名 | `wb_switcher_hub` / `wb_switcher_page_<view>` / `if(window.self!==window.top) return` | **反钉**：防止将来有人把"有意设计"当成重复给"统一"掉 |

最后一条尤其重要 —— 第十七章刚勘误过「频道名不一致不是缺陷」，
这条断言就是防止下一个人重犯同一个误判。

### exe 重打与验证

```
WB EXIT=0  dist/WorkBuddySwitcher/WorkBuddySwitcher.exe  09-24 09:10
TW EXIT=0  dist/TraeSwitcher/TraeSwitcher.exe            09-24 09:11
```

⚠️ 验证时碰到一个**认知点**：`wb_ui_app` / `tw_ui_app` 是 **CArchive 的入口脚本条目**，
**不在 PYZ 里** —— 走前几轮那套"解 PYZ"的探针会找不到它们。
直接 `CArchiveReader.extract('wb_ui_app')` + `marshal.loads` 即可：

| 包 | `_port_alive` | `_wants_server_cli` | `_SERVER_ACTIONS` |
|---|---|---|---|
| `WorkBuddySwitcher.exe` | **False**（已删） | True | True |
| `TraeSwitcher.exe` | **False**（已删） | True | True |

前端两个 html 与源码 md5 仍逐字节一致（`7fc3c00c` / `168b9843`）。

### 最终状态

**41 个编号项：40 项已修 + 1 项（P3-18 的"抽 `ui_common.js`"重构部分）明确不做，并已用断言替代。**

另有 2 项**无编号**遗留（Trae `refresh_all()` 无门卫、Trae `20403`）用户决定不处理；
`.trash_20260914/` 用户决定保留。

---

## 十九、P2-6 加固：等锁超时 + 行为级验证（2026-09-24 09:16）

**起因**：自查五轮改动时发现 P2-6 新加的回源锁**是在持锁期间做网络请求的**，
而 `_billing_post(url, headers, timeout=20, retries=2)` 单账号最坏 **40 秒** ——
20 个账号 6 并发最坏可达 ~160 秒。锁若没有上限，后来的请求会被一直挂住。

### 改动：等锁加超时，超时退化为自己扇出

新增模块常量 `FETCH_LOCK_TIMEOUT = 30.0`；`credits_snapshot` / `checkin_snapshot`
两处把 `with _fetch_lock(...)` 改成显式 `acquire(timeout=...)` + `try/finally`
（`got=False` 时**不 release**，因为压根没拿到）：

```python
_lk = _fetch_lock("credits", ch.key)
_got = _lk.acquire(timeout=FETCH_LOCK_TIMEOUT)
try:
    ...重查缓存 / 扇出 / 写缓存...
    return payload
finally:
    if _got:
        _lk.release()
```

**为什么值得加**：锁的价值是"同一通道只扇出一次"（正常路径 1~2 秒就填好缓存，
第二个请求秒回）。但持有者若卡在慢网关上，**不设上限就等于把后来的用户挂死**。
超时后退化为改造前的并发行为 —— 宁可多打一次网关，也不卡住用户。

### 行为级验证（本轮重点）

之前 P2-6 **只有源码断言**，没有行为验证。这次补了 4 条：桩掉 `query_credits`
（sleep 0.3s 模拟外部请求）后**真跑并发**：

| 断言 | 结果 |
|---|---|
| 同通道并发 `force=True` 两次 → 底层查询只跑**一轮** | **2 次**（2 账号；若 single-flight 失效会是 4 次）✅ |
| 两个并发调用都拿到结果 | ✅ |
| 手动占住锁 + 把超时临时改成 0.3s → 第二个请求**不挂死** | 耗时 **0.60s**（0.3 等锁 + 0.3 扇出）✅ |
| 超时路径确实自己扇出了 | **2 次**查询 ✅ |

**回归自检**：`smoke_test.py` **591 PASS / 0 FAIL，退出码 0**（上一轮 587 → **+4 条**，全部是行为级）

### exe 重打

`wb_ui_server.py` 是**国服专属**，所以按需**只重打 WorkBuddySwitcher**：

```
WB EXIT=0  dist/WorkBuddySwitcher/WorkBuddySwitcher.exe  09-24 09:16
           dist/TraeSwitcher/TraeSwitcher.exe            09-24 09:11（未变，本轮无需重打）
```

包内验证（解 PYZ）：`wb_ui_server` 里 `FETCH_LOCK_TIMEOUT` / `acquire` / `release`
**三者都在**。

---

## 二十、端到端验证与新发现（2026-09-24 09:22，实例已启动）

用户启动实例后 `/api/ping` 报 `version: wb-20260924-091508` ——
与 `wb_ui_server.py` 的 mtime（09:15:08）**精确吻合**（`APP_VERSION = common.source_version(__file__, "wb")`
取的就是该文件 mtime），**确认跑的是含六轮全部修复的版本**。

终于能做端到端验证了（前六轮全是静态验包）。结果 **17/19 PASS**，两条 FAIL 查下来
**一条是探针写错、另一条挖出一个新问题**。

### 端到端验证通过项

| 项 | 验证结果 |
|---|---|
| P2-2 页面路由在 `_guard()` 之后 | 跨站 Host 取 `/`、`/wb`、`/wbai` 全 **403**；本机取 `/` → 200 |
| P2-2 有意豁免 | 跨站取 `/api/ping`、`/api/page-alive` 仍 200 |
| P2-1 坏 Content-Length | `abc` / `-5` / `1GB` 全 **400** |
| 功能接口 | `/api/accounts` 200（8 个账号）、`/api/current` 200 |
| P3-19 | 渲染出的入口页含 `location.origin` 填地址逻辑 |
| P2-10 / P1-6 / P3-17 / P3-20 | 渲染出的 `/wb` 页分别含就绪守卫 / `c.live` / 新 `tail` 逻辑 / `_renderPending` |

### FAIL ①：探针写错（非缺陷）

断言「首页注入了访问令牌」用的是 `b"SWITCHER_TOKEN" in body` —— 但
**前端代码里本来就有 `window.SWITCHER_TOKEN` 的引用**（`ui_template.html:510` 用它拼请求头），
所以这个子串无论注不注入都存在。要验注入必须匹配 `window.SWITCHER_TOKEN="..."` 这种**赋值**形式。

### FAIL ②：**新发现** —— 桌面启动器缺一次性令牌

追这条时发现：`POST /api/switch` 带合法请求体、**不带令牌**，竟然**没返回 401**，
而是直接进到 `api_post` 返回业务错误。查下来：

| 启动路径 | 是否设 `Handler.TOKEN` |
|---|---|
| `wb_ui_server.serve()`（CLI / `--serve` 路径） | `common.new_token() if use_token else None` |
| `tw_ui_app.py:154`（TraeSwitcher 桌面启动器） | **设了** `srv.Handler.TOKEN = common.new_token()` |
| **`wb_ui_app.py`（WorkBuddySwitcher 桌面启动器 / exe 入口）** | **完全没有任何 TOKEN 代码** |

`wb_ui_app.py` 走的是 `common.bind_server(srv.Handler, port)`，**不经过 `serve()`**，
所以 `use_token` 那段逻辑根本没执行 → `TOKEN` 保持类默认值 `None` →
`do_POST` 里 `if self.TOKEN and ...` 恒假 → **令牌校验被整段跳过**。

**影响**：

1. 写端点少一层纵深防御。**不是可直接利用的漏洞** —— `_guard()` 的 Host+Origin 校验仍在
   （上面已验证跨站一律 403），DNS rebinding 也过不了 Host 校验；
2. **两个启动器行为不一致** —— 与 P3-14「重复实现漂移」同一族。

**修法**（一行）：`wb_ui_app.py` 在 `bind_server` 之前加
`srv.Handler.TOKEN = common.new_token()`。前端 `ui_template.html:510` 已经会回传
`X-Switcher-Token: window.SWITCHER_TOKEN || ''`，所以补上后写操作不会 401。

### 已修（09:26）

用户确认补上。改动：

- `wb_ui_app.py`：在 `common.bind_server()` **之前**加 `srv.Handler.TOKEN = common.new_token()`，
  并写清为什么必须显式设（本文件不走 `serve()`）。
- `smoke_test.py`：新增 3 条断言 —— 两个启动器都要有这行、必须在 `bind_server` 之前、
  且确认它们走的是 `bind_server`（所以不能指望 `serve()` 里的 `use_token` 逻辑）。

**回归自检**：**594 PASS / 0 FAIL，退出码 0**（上一轮 591 → **+3**）
**exe 重打**：WorkBuddySwitcher 09:26（TraeSwitcher 无需变）。

包内验证（入口脚本在 CArchive 里，**必须递归**找 `co_names` —— 顶层没有，因为代码在 `main()` 里）：

| 包 | `TOKEN` | `new_token` | `bind_server` |
|---|---|---|---|
| `WorkBuddySwitcher.exe` | ✅ | ✅ | ✅ |
| `TraeSwitcher.exe` | ✅ | ✅ | ✅ |

⚠️ **这条改动要重启实例才生效** —— 用户当前跑的是源码方式（`python.exe` pid 28932），
重启即加载新代码。

### 重启后验证：令牌确实生效了（09:34）

用户重启（新 pid **15420**，`started_at` 换算约 09:33:36）：

| 请求 | 结果 |
|---|---|
| POST `/api/switch` **不带**令牌 | **401** `缺少或错误的一次性访问令牌` ✅（修复前是 200 + 业务错误） |
| POST `/api/switch` **带错**令牌 | **401** ✅ |
| 坏 Content-Length | **400**（在令牌校验**之前**，与设计一致）✅ |

### 顺带发现：版本戳覆盖不全（源码态）

重启后 `version` **仍是** `wb-20260924-091508` —— 因为
`APP_VERSION = common.source_version(__file__, "wb")` 只取 **`wb_ui_server.py`** 的 mtime，
而这次改的是 `wb_ui_app.py`（09:26）。**光看 version 无法确认启动器更新是否生效**，
上面那条是靠**行为**（401）验证的。

`source_version` 的实现（`switcher_common.py:515`）：只 stat 传入的 `path`，失败才回退
`sys.executable`。所以：

- **打包态**：`__file__` 指向解包目录、stat 不到 → 回退到 exe 本身 → 戳反映**重打时间** ✓
- **源码态**：只看 `wb_ui_server.py` → **改启动器不改变版本戳** ✗

影响：`wb_ui_app.py:153` 的 stale 检测（"那个在跑的实例是不是旧代码"）在源码态下会漏判。
属 **P3（诊断能力）**。

### 已修（09:47）

用户确认修。改动：

- `switcher_common.source_version(path, tag, extra_paths=())` —— 新增 `extra_paths`，
  参与**取最大 mtime**；候选全都 stat 不到时才回退 `sys.executable`（打包态语义不变）。
- `wb_ui_server.py` / `tw_ui_server.py` 的调用处各把自家启动器传进去
  （`Path(__file__).with_name("wb_ui_app.py")` / `"tw_ui_app.py"`）。打包态下这个路径
  stat 不到，会自动跳过、照旧回退到 exe，**不影响打包态行为**。
- `smoke_test.py`：+6 条断言，其中 3 条是**行为级** —— 用两个临时文件造出 100 秒 mtime 差，
  验证"传了 extra 之后戳跟着更新那个文件走"。

**回归自检**：**600 PASS / 0 FAIL，退出码 0**（上一轮 594 → **+6**）
**exe 重打**：WB 09:47 / TW 09:48（`switcher_common.py` 是共用的，两个都要重打）。

包内验证：`switcher_common` 里 `extra_paths` ✅；两个 `*_ui_server` 里
`source_version` / `with_name` ✅。
（`extra_paths` 作为**关键字参数名**不进 `co_names`，所以直接查它查不到 —— 用 `with_name`
判定调用方是新代码。）

⚠️ **要重启实例才生效**（用户跑的是源码方式，重启即加载）。

### 顺带记一个坑

验入口脚本时，第一版只查**顶层** `co_names`，`TOKEN` / `new_token` / `bind_server`
**全是 False**（连 `bind_server` 都 False 才意识到不对）——
因为它们都在 `main()` 函数体内，属于嵌套 code object。
验入口脚本里的函数体必须**递归**遍历 `co_consts` 里的 code object。

---

## 二十一、启动器合并重构（2026-09-25 09:37，按用户要求）

用户要求：① 删除 `TraeSwitcher.spc`（笔误，实为 `.spec`）；② `wb_ui_app.py` → `ui_app.py`；
③ 预留设计以便扩展支持 Trae Work 和 Trae。

**动手前确认三点**（AskUserQuestion）：确定删 `TraeSwitcher.spec`、**合并成一个 `ui_app.py`**、
**Trae Work 与 Trae 是两个不同产品**。

### 结果

- **新建 `ui_app.py`**：产品差异全部集中在 `_PRODUCTS` 表（`title` / `server_module` /
  `mutex_attr` / `root_marker` / `cli_actions` / `window` / `rebind_root` / `warmup` /
  `status` / `note`）。**新增产品 = 加一条记录**，不必再复制一份 200 行启动器。
  - 已实现：`wb`（默认）、`trae`
  - **预留占位**：`trae-work`（`status="planned"`，启动时明确拒绝，`note` 里列出还差什么）
- **删除**：`wb_ui_app.py`、`tw_ui_app.py`、`TraeSwitcher.spec`
- **`WorkBuddySwitcher.spec`**：入口改 `ui_app.py`；hiddenimports 加
  `wb_ui_server` / `tw_ui_server` / `trae_work_checkin`
- **引用同步**：两个 server 的 `extra_paths`、`smoke_test.py` **11 处**、
  `README.md` / `README_Trae.md` / `DESIGN_single_instance.md`

### 踩到的两个坑

**① `wb_ui_server` 没进 PYZ（严重）** —— 改成 `importlib` **动态导入**后静态分析扫不到，
必须在 `hiddenimports` 显式登记。它以前是**顶层 import**、能自动扫到，改成动态导入后就漏了。
**靠重打后解包查 PYZ 才发现**（`wb_ui_server: False`，而 `tw_ui_server: True`）。

**② `_die` 弹框阻塞（本次引入）** —— `notify_user` 弹的是原生消息框，**阻塞**到用户点确定；
实测 `exe --product nope` 无人值守时**永不返回**。改成后台 daemon 线程 + 10 秒限时等待，
修后实测 **rc=2、耗时 10.3~10.5s**。

### 验证

- **`smoke_test.py` 600 PASS / 0 FAIL**
- 解包：入口条目 **`ui_app`** ✓；`wb_ui_server` / `tw_ui_server` / `switcher_common` /
  `trae_work_checkin` / `workbuddy_checkin` / `account_migration` **全在** ✓；
  `wb_ui_app` / `tw_ui_app` **已不在** ✓
- **实测产品分流**：`--product wb --current` rc=0、`--product trae --list` rc=0
  —— Trae 侧在**同一个 exe** 里跑通（合并的核心目标）

### 遗留

- exe 名 `WorkBuddySwitcher` 已不贴切（现在也能起 Trae），用户未要求改
- `dist/TraeSwitcher/` 旧产物未删


